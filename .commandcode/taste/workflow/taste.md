- Wants visual/interactive UI layer over pure CLI workflows — asked for "giao diện đẹp" (nice interface) and the assistant built a Rich TUI app for managing the project; values having a polished interface for tasks like status checking, audiobook creation, and git ops rather than remembering raw commands. Confidence: 0.8
- Prefers desktop GUI apps over web apps (Streamlit) or pure TUI — originally accepted PyQt6, then explicitly migrated to C# WPF for Windows-native performance; still values TUI for quick terminal tools but wants a full desktop app for primary workflows. Confidence: 0.8
- Prefers "Liquid Glass" aesthetic for UI — explicitly requested "giao diện UI đẹp phong cách Liquid Glass màu sắc tươi mới" (beautiful UI with Liquid Glass style, fresh colors); likes glass morphism, soft gradients, semi-transparent elements, and vibrant color palettes. Freshly reconfirmed: shared Windhawk theme configs (Glass 3.0 Taskbar + File Explorer glass) as design reference for upgrading the WPF desktop app — gradient borders, blur tints at multiple opacity tiers (#10ffffff–#40ffffff), large corner radius (12–22px), state-specific opacity changes, vertical gray gradient borders, per-control blur tint levels (tab normal/selected/hover, search box, element backgrounds) — and expects the assistant to interpret multiple external theme configs and merge them into a single coherent WPF XAML design system across all views/styles; provides theme sources incrementally (Taskbar first, then File Explorer) expecting the assistant to accumulate and integrate each addition into the existing LiquidGlass.xaml. Confidence: 0.92
- Text readability is a hard constraint within the Liquid Glass aesthetic — pushed back THREE times ("chữ vẫn mờ lắm", "chữ vân mờ lắm", "chữ trong các thẻ cơ mờ lắm") on glass backdrop opacity being too low; increasing white-tinted opacity alone (#30→#60ffffff) never solved it because white-tinted glass washes out light text regardless of opacity level. The correct approach is DARK-TINTED semi-transparent glass (e.g. `#501a1a2e` — dark blue-black with alpha) rather than white-tinted glass (`#60ffffff`); dark backgrounds make light/white text inherently readable while still allowing the blur effect to show through. The assistant should default to dark-tinted glass colors for all glass backdrop elements and err on the side of higher contrast when tuning, rather than iterating through progressively more opaque white tints. Confidence: 0.92
- Prefers simple launch methods (e.g., .bat files for double-click) over remembering terminal commands — asked "tôi mở kiểu gì nhỉ" (how do I open it?) and the assistant proactively created an app.bat for one-click launch; indicates comfort with UI tools but not with remembering CLI invocations. Freshly reconfirmed for newly-installed tools: right after lazygit was installed via winget, the user asked "mở lazygit như thế nào" (how do I open lazygit?) — expecting plain, step-by-step launch guidance (open a new terminal, type the command, or the direct exe path fallback) rather than figuring out the invocation or PATH caveats themselves. Confidence: 0.85
- Wants additive-only changes when building on existing stable architectures — explicitly said "đặc biệt ko phá vỡ cấu trúc logic đang rất ổn của dự án" (especially don't break the already-stable project structure); expects new modules/features to be added as entirely new files/directories without modifying existing pipeline scripts; values architectural stability highly. Confidence: 0.9
- Requires backward compatibility with existing data formats and completed work when refactoring — explicitly scoped changes so existing progress JSON formats are not altered ("Không thay đổi format progress JSON hiện có để bảo đảm tương thích với merge_chunks.py và các sách đã hoàn tất"), old progress without metadata still recognized on resume, and merged/completed books remain valid; new coordination layers (manifest, cache fingerprints) must be additive guards, never a new source of truth that breaks old data. Confidence: 0.75
- Wants API key integration for automated translation — explicitly requested adding API key support (Gemini, DeepSeek, OpenAI-compatible) so the assistant can call APIs directly for translation instead of relying solely on the chatbot agent; values the convenience of API-driven pipelines. Confidence: 0.85
- Wants real-time progress/status display in UI — explicitly asked for "khu hiện trạng thái chạy nữa nhìn cho nó nguy hiểm" (status area that looks impressive); wants visible progress tracking with animated indicators showing translation status, not just static info. Freshly reconfirmed when the user noticed the restyled indeterminate progress bar in the Realtime Log had NO sliding effect ("sao ko có cái hiệu ứng trượt khi đang hoạt động vậy" — why is there no sliding effect when it's active?): a busy/indeterminate indicator must actually ANIMATE (a glow bubble sliding back and forth, like the WPF default ProgressBar's `PART_GlowRect`), not merely change color; when a custom control template replaces a WPF control's default template, the built-in animated behaviors must be re-implemented in the custom template or the user will flag the missing animation as a defect — the accepted fix keeps the sliding bubble only while `IsIndeterminate=True` (indicator hidden, bubble visible with a forever-loop TranslateTransform) and restores the static indicator for real progress values. Confidence: 0.88
- Progress/indeterminate bar animation ranges must be sized to the actual visible width of the control, never a fixed hard-coded travel distance — when the sliding glow bubble was animated with `To="480"` while the Realtime Log track was only ~60px wide, the user flagged it as a defect ("hiệu ứng có vẻ ok nhưng thanh trượt nó đang ở đâu ấy kiểu mãi mới thấy nó trượt ngang có thể bạn để hơi dài" — the effect seems ok but the slider is somewhere out there, you only glimpse it sliding past, maybe you made it too long), because the bubble spent most of its cycle outside the visible area; the accepted fix replaced the width-dependent TranslateTransform with a ScaleTransform pulse (ScaleX 0→1→0, center origin, AutoReverse) whose stretched gradient fills the whole track — always visible, and self-adapting to any track width (~60px log vs ~280px book card) with zero per-control tuning. Confidence: 0.8
- The indeterminate/busy loading animation must be the CLASSIC left-to-right repeating light streak, not a center-outward scale pulse — when the pulse was delivered, the user rejected it outright: "trượt từ tâm ra à sao không làm trượt từ trái qua phải xong lại lặp lại như thế nhìn cho nó nguy hiểm" (slides from the center? why not slide left-to-right then repeat, so it looks impressive); the settled pattern is a `LinearGradientBrush` with `RelativeTransform` (TranslateTransform X animated -1 → 2 in relative units) carrying a bright gradient streak through transparent regions, so the light sweeps in from the left edge, crosses the track, exits right, then loops back to the start (RepeatBehavior Forever, no AutoReverse) — keeping the width-adaptive property via relative units instead of pixel distances. Confidence: 0.85
- The indeterminate loading streak must sweep SLOWLY and be LONG (spanning most of the track width) — the first left-to-right streak was judged "cái phần trượt hơi nhanh và ngắn" (the sliding is a bit fast and short); the accepted tuning slows the pass (1.1s → 1.8s per sweep), widens the visible gradient light band so it covers most of the bar, reduces the travel range slightly (From −1.2 To 1.8 instead of −1→2) so the streak doesn't vanish too long at either end, and drops ease ratios to ~0.1 for a steady, non-jerky sweep. The length was iterated in three steps: first widened to ~60% of the track (gradient stops 0.15–0.75), then, when the user approved the look but asked for just a bit more ("đẹp ấy cho dài ra xíu nữa là được" — it's nice, make it a bit longer), the value became ~70% of the track width (bright stops 0.05 → 0.7, fading to 1.0), and a third nudge ("dài thêm một tý nữa" — make it a bit longer again) pushed the streak to span nearly the ENTIRE bar: bright band starting at offset 0.02, peak cyan 0.18, purple 0.38, fading out to 1.0, at the same 1.8s sweep — a streak that covers almost the whole track width is the settled final value. Confidence: 0.95
- Prefers to keep stable/configured settings unchanged when they work — when told the prompts directory is functional and not broken, said "giữ nguyên thư mục này đi không cần đổi đâu" (keep it, no need to change); avoids unnecessary changes to things that already work. Confidence: 0.8
- Prefers careful verification and merging over outright deletion of legacy code — explicitly said "kiểm tra kĩ nhé xem nếu mà cần thiết thì gộp vào nhé" (check carefully, if needed merge it in) before authorizing removal of deprecated scripts/api/; wants thorough analysis of whether legacy code has useful functionality worth consolidating before discarding. Confidence: 0.8
- Wants UI to show complete picture of all items in the pipeline, including pending/input — expects the book list to display both already-processed output books AND untranslated input files; doesn't want incomplete views that only show one stage of the workflow. Confidence: 0.85
- Prefers tabbed/separated UI over mixed lists when items have distinct states — finds combined lists "khó kiểm soát" (hard to control); explicitly asked to split book list into Input tab and Output tab rather than showing everything in one scrollable list; values clear categorical separation in UI. Confidence: 0.85
- Prefers UI action buttons grouped by workflow stage/state of the item they operate on — processing/transformation actions (Dịch, Gộp, EPUB, QA) belong on the Input tab (raw/unprocessed books), while the Output tab (finished/translated books) should only carry consumption actions (play audio, read the file); explicitly complained that the Output card showed Dịch/Audio/Gộp/EPUB/QA buttons and said "thế mới đúng logic chứ" (that's the correct logic). Confidence: 0.85
- When redundant UI controls are found, prefers removing them over duplicating/moving them elsewhere — chose "Chỉ bỏ khỏi Output" (just remove from Output) over moving the Dịch/Gộp/EPUB/QA buttons into Input, because the Input pipeline already performs those steps; avoids duplicate controls that overlap existing functionality. Also flagged the standalone "Audio" button on the Output card as redundant ("trong phần đọc thử có sẵn audio rồi mà") because the Đọc thử preview window already embeds an audio player and the sidebar Audio page already creates audiobooks — expects the assistant to check for existing feature coverage before keeping/adding a control, leaving a single clear action per card. Confidence: 0.85
- Wants todo lists cleared promptly after task completion — finds lingering/stale todos visually annoying ("cứ hiện nhìn khó chịu"), wants them cleaned up immediately rather than left on screen. Confidence: 0.85
- Prefers Windows-native UI frameworks (C# WPF) over cross-platform Python frameworks (PyQt6) when the project is Windows-only — explicitly chose to migrate the entire desktop app from PyQt6 to C# WPF for native look, lower memory (~50MB vs ~200MB), faster startup, and genuine Liquid Glass animations; willing to accept slightly slower dev speed for better runtime quality. Confidence: 0.85
- Prefers clean architecture separation by language — explicitly chose C# for UI layer only while keeping Python for pipeline/scripts; expects the best tool for each job rather than a single-language monolith; the C# app calls Python via subprocess for pipeline operations. Confidence: 0.85
- The project has TWO parallel operation streams and the user thinks in terms of both — Stream 1 (`/dich`, `/dich_auto`, `/audio`, executed via the chat Agent) is "chạy rất ổn rồi" (running very stably now) and is the production path; Stream 2 is the Windows WPF desktop GUI (TranslateBook.exe) calling the API directly (Gemini/Deepseek) for the same work, currently being piloted: "luồng thứ 2 là sẽ dụng UI nghĩa là giao diện windows giao diện windows thì ko thể liên kết AIgent được nên sẽ sử dụng API" (stream 2 is the Windows UI — it can't connect to the Agent, so it uses the API). The user explicitly framed UI features as a stability pilot — "tôi đang muốn thí điểm luồng 2 để xem là có ổ định không" (I'm piloting stream 2 to see if it's stable) — and the request to add a "nút demo dịch thử" (demo translate button) for the UI is the early validation step for that pilot, not a request to abandon Stream 1. For the agent-vs-API choice per feature, prefer the agent as the engine when the user is in the chat (Stream 1 context), but build the UI as API-backed from the start when the feature must live inside the desktop GUI (Stream 2 context, where the agent cannot be invoked) — i.e. the choice is driven by which stream the work runs in, not by a global preference for one over the other. Fresh evidence from the first real pilot of Stream 2 (the API-backed desktop "Dịch Toàn bộ" run): the user judged it "cảm giác rất là lâu ... không mượt như Agent làm /dich" (feels very slow, not as smooth as when the Agent runs /dich), explicitly benchmarking the desktop API path against the agent-driven path and reaffirming the agent-run `/dich` as the smooth production standard for whole-book translation. Freshly reconfirmed with a standing demand: when the assistant offered to abandon the desktop path and run `/dich` for the book instead, the user reiterated "làm sao để nút dịch toàn bộ này mượt như /dich đi" (make this translate-all button as smooth as /dich) — the Stream 2 button must be ENGINEERED to approach the agent standard, not accepted as a permanently slower pilot or sidestepped by rerouting work to Stream 1; the `/dich` command's mechanics (bigger chunks, batching, single continuous context, no per-chunk HTTP) are the reference spec the assistant is expected to reverse-engineer and propose porting. Reconfirmed a third time when the user attributed the agent path's smoothness AND near-total connection stability specifically to its subagent architecture — "kiểu khi mà dịch thôi thấy Agent chia nhiều subagent mượt lắm và gần như ko bao giờ bị mất kết nối" (when translating, the Agent splitting into many subagents is very smooth and almost never loses connection) — i.e., the user's mental model is that `/dich`'s reliability comes from many independent subagents running inside Command Code's own managed infrastructure (internal retry/connection handling), not from anything the translation code or HTTP client does; when explaining or engineering Stream-2 parity, the assistant should engage at that architecture level (reuse/spawn Command Code's own agent infrastructure — the assistant verified its headless mode `cmd -p ... --yolo` exists as a path for a desktop app to delegate batches to — rather than only tuning the desktop's raw per-chunk HTTP requests). Confidence: 0.92
- The primary/current translation workflow runs through the AI Agent directly, not API calls — explicitly said "bình thường sẽ dùng AI Agent" (normally I use the AI Agent), "tôi không dùng API đâu" (I don't use API), and reaffirmed "chức năng API sẽ phát triển sau" (API features will be developed later); API is only planned later to serve the desktop software, so performance/optimization work should target the agent loop (fewer round trips, batching chunks, less re-reading of files) rather than HTTP concurrency/retry tuning. This agent-as-engine principle extends to NEW AI-powered features, not just translation: when the assistant asked the user to supply a Deepseek API key to run the AI per-chapter music suggestion (level B), the user pushed back "sao lại cần API key nữa nhỉ, tôi đang dùng chat Agent mà" (why do I need an API key again, I'm using the chat Agent) — the chat agent itself must be the engine, i.e. the agent reads chapter content, scores mood/energy, and produces the per-chapter music map (JSON) the pipeline consumes, rather than requiring the user to set up or obtain external API keys; the assistant was expected to pivot the design accordingly. Freshly reconfirmed: after the user ordered the AI per-chapter music analysis, the assistant again proposed having the user supply a Deepseek API key (or a free local heuristic as an alternative), and the standing expectation is that the agent itself performs the analysis using the existing project's agent-driven workflow — the user should never be asked to obtain/set up an API key for a feature the chat agent can run directly. Confidence: 0.98
- Requires a hard maximum of 2 analyzer–executor review rounds to prevent infinite feedback loops: round 1 is Analyzer plan → Executor implementation → Analyzer review; if the result is `NEEDS_CHANGES`, exactly one additional Executor fix → Analyzer review (round 2) is allowed, after which orchestration must terminate regardless of the second review result. This is a limit on coordination/review rounds, not a character or token limit, and must not be interpreted as an unlimited retry count or as two fix iterations beyond the initial implementation. Confidence: 0.98
- Wants the orchestrator (Agent 1) to wait for the executor (Agent 2) with NO time limit — explicitly requested "Agent 1 đợi Agent 2 vô hạn thời gian" (wait indefinitely) while keeping the review loop capped at 2 rounds; the 2-round limit is a limit on the NUMBER of review rounds, not a limit on waiting time. The orchestrator should never impose its own timeout or cancel a slow-but-active executor, and must not spawn a replacement/retry executor while the current one is still running — only a self-reported `FINAL_STATUS: BLOCKED` or natural completion ends the wait. (Analyzer keeps its own protective turn limit.) Confidence: 0.95
- Prefers multi-model specialization for agent roles — wants different models linked together for different agent functions (e.g., Claude Opus for deep analysis/review, GPT/Codex for coding/execution) rather than using the same model across all agents. Confidence: 0.85
- Wants token/cost optimization to be balanced with output quality — reduce redundant context and use cheaper models where safe, but retain the evidence and context needed for accurate analysis, implementation, and review; does not want cost savings achieved by mechanically cutting essential information. Confidence: 0.9
- Identifies the review/analyze step as a specific cost-optimization target — the Luna review phase costs ~$2.6 per run, and substituting the free Laguna model for review (while keeping analysis/planning on a capable model) is the preferred lever for cutting dual-agent costs; however, prefers maintaining a **separate reviewer agent** running the free model rather than executor self-review, to preserve dual-agent independence/objective review. Confidence: 0.7
- Prefers role separation between a read-only analyzer agent and a write-capable executor agent — the analyzer only analyzes, creates implementation plans, and reviews output (never writes files), while the executor only implements and writes code; this clean separation prevents the reviewing agent from directly altering implementation, and the user enforces it at the tool level by stripping any potentially-mutating tools (e.g. shell_command) from the analyzer so read-only is guaranteed, not just conventional. Confidence: 0.85
- Orchestrates multi-agent workflows via slash commands with `agent: build` frontmatter — the orchestrator slash command runs on the main session and synchronously calls specialized sub-agents via `agent()` calls, passing structured context between them through the orchestration logic. Confidence: 0.8
- Requires explicit slash-command activation for multi-agent workflows (e.g., `/dual-Agent <task>`) — does NOT auto-trigger based on automatic detection of complex tasks; typing the task alone runs in normal single-agent mode. The dual-agent review loop only runs when the user explicitly invokes the slash command. Confidence: 0.95
- Verifies that planned slash commands are actually registered in the Command Code runtime by inspecting the command directory (file presence + directory listings to confirm autocomplete availability), not just by reading the plan document; asks for repeated re-checks to confirm current runtime state before relying on a command. Also expects FUNCTIONAL verification that the commands run correctly, not only registration: valid frontmatter (description + agent: build), referenced scripts actually exist on disk, and every CLI flag written into the command docs is accepted by the real script (validated via `--help`) — freshly reconfirmed when, right after the pipeline was split into `/dich`, `/audio`, `/dich_audio`, the user tersely ordered "kiểm ta lại các lệnh đó xem chạy đúng chưa nhé" (check those commands again to see if they run correctly), triggering a verification pass across both registries. Confidence: 0.9
- Prefers fully automatic, hands-off multi-agent orchestration — wants a single command to trigger the entire agent pipeline (analyze → execute → review → loop) with zero manual intervention or step-by-step agent invocation; the user explicitly checked whether agents "work automatically together" or require manual management and expects confirmation that the orchestrator handles all iteration counting, context forwarding, and agent calls on its own. Confidence: 0.95
- Wants the delegated executor to behave like a genuinely autonomous agent, not merely an interface that relays commands to the main assistant — it should independently complete the full approved task, including filesystem operations such as moving/deleting files and repository checks, with appropriate safety controls. Explicitly restated: "tôi muốn nó như một Agent thực thụ bản chất là chỉ là thay tôi ra lệnh thôi mà" (I want it as a real Agent, essentially just issuing commands on my behalf) and asked whether it can create/move files in the project — the user expects the agent to do the work so they only have to give one order. Confidence: 0.95
- Describes the desired dual-agent flow explicitly as a closed loop with the analyzer's plan as the authoritative source of truth: the user issues one command → Agent 1 (analyzer) analyzes and produces the plan (the "ý tưởng"/idea) → Agent 2 (executor) implements exactly per that plan → Agent 1 reviews whether the result matches the original idea → the completion result ("đã hoàn thành") is reported back to the user. The executor must follow Agent 1's plan precisely and the final review verifies fidelity to the original idea before reporting completion — not ad-hoc manual steps in between. Confidence: 0.8
- Prioritizes correctness/completeness over raw speed when parallelizing translation — worried whether parallel translation would "dễ bị sót hay bị loạn" (easily miss or scramble content); expects any parallel scheme to guard against content loss/duplication, style/terminology drift, broken context, and file overwrites, e.g. parallelize by independent chapter groups with non-overlapping per-agent chunk manifests, a fixed shared glossary/rules, per-chunk writes, and completeness/order/terminology verification before merge; prefers one agent handling a full chapter rather than splitting mid-chapter. Confidence: 0.65
- Applies the same safety-first, conservative approach to audiobook/TTS optimization as to translation — accepts bounded parallelism (e.g. 2 workers by chapter) only with sufficient RAM/CPU, and only after benchmarking; preserves checkpointing to avoid re-generating finished audio, and prioritizes keeping existing stable settings (temperature=0.3, top_k=10) over speed gains that could hurt voice quality. A proposal explicitly deferring parallel TTS until after measuring RAM, RTF, and real voice quality was approved as-is. Reconfirmed with an explicit, direct instruction to optimize speed: "bạn thử test thử xem đẩy nhanh tốc độ nhưng chất lượng vẫn phải đảm bảo cho tôi nhé" (go test speeding it up, but quality must still be guaranteed) — this authorizes empirical performance experimentation (e.g. sweeping `--batch-size` 4/8/16/32 on real chapter chunks with unchanged sampling params) and expects quality to be verified acoustically (RMS, spectral centroid, duration) before any conclusion, with the adopted config proposed for approval before re-running the full book. Reconfirmed with a second explicit quality-guarantee instruction when authorizing the full-book batch-16 re-run: "nếu chạy nhanh nhưng vẫn phải giữ nguyên chất lượng audio cho tôi đấy" (if it runs faster, you must still keep the audio quality) — speed gains are only acceptable with the sampling parameters untouched and quality verified. Confidence: 0.95
- Standardized the GPU TTS parameters for ALL future audiobook books after the 08-13 benchmark: use `--gpu --batch-size 16` (RTF 0.12, ~2x faster than the old batch-size 8) instead of batch-size 8, while keeping the sampling params fixed at temp 0.3, top_k 10 — the user declared this explicitly as "Chuẩn GPU TTS cho mọi sách sau" (the GPU TTS standard for all books after this), stating quality is equivalent with no degradation, so new-book audiobook runs should default to these flags without re-benchmarking or re-asking. Confidence: 0.9
- Periodically checks in on audiobook generation speed with a terse question ("tốc độ tạo file audio này đã ok chưa" — is the audio generation speed okay yet?) and expects an evidence-backed performance verdict rather than a guess: a real sample benchmark (measured RTF, e.g. 0.77 on a ~287-char chunk) PLUS end-to-end RTF computed from the progress JSON (total_gen_time/total_audio_time, e.g. 5068s/11399s ≈ 0.44), compared against the machine's known baselines (GPU RTX 3060 RTF ~0.15 vs CPU ONNX ~0.42), an honest conclusion on acceptability (e.g. "acceptable but not optimal"), a root-cause breakdown of any gap (effective batch size, CPU-bound music-mixing/normalize/MP3-convert steps excluded from gen_time, model warm-up/load), and concrete optimization options (raise --batch-size 8→16 if VRAM allows, measure pure-generation RTF to find the true bottleneck) that are only executed after explicit user approval. Reconfirmed when the user followed the verdict by explicitly ordering the test: "bạn thử test thử xem đẩy nhanh tốc độ nhưng chất lượng vẫn phải đảm bảo cho tôi nhé" (try testing to speed it up, but keep the quality guaranteed) — a terse sentence is enough to authorize the assistant to run the full experiment autonomously (check GPU headroom, read the TTS library source for the batching mechanism, write/run a benchmark script, verify quality with acoustic metrics, clean up test artifacts) and then stop to propose the winning config (batch 16 ≈ 1.9x faster, quality equivalent) for approval before re-running all chapters. Confidence: 0.7
- Diagnoses TTS audio defects by verifying each layer before changing parameters — when the user reported sentences read twice, the assistant first verified the source markdown had no duplicate lines, then verified `smart_chunk` produced no duplicate/overlapping chunks, and only after ruling out the text layer concluded the repetition came from TTS model hallucination (repetition_penalty only spans tokens within a single chunk's history); expects root-cause evidence (rule out source/chunking → attribute to model → then adjust generation parameters) rather than tuning parameters blindly. A further root cause discovered for TTS sentence repetition is text-structure-driven over-chunking: when the source markdown puts each dialogue line on its own line separated by blank lines, the chunker produces isolated ~20-30 char chunks with no context, and the model repeats/hallucinates (the model can even brag extended new content from a single short sentence — one 23-char sentence produced 67.7s of audio); the fix is to merge adjacent small chunks/paragraphs (dialogue + narration) into context-rich chunks so the model never sees a lone short line. The finalized fix (08-14, verified across all 12 chapters): `extract_chapter_text` merges short paragraphs (<50 chars) into the previous paragraph, and `smart_chunk` merges short chunks (<50 chars) in BOTH directions (forward pass, then a backward pass into the following chunk), allowing the merged chunk to exceed max_chars up to TTS_MAX_CHARS (320) because a lone short chunk is a worse risk than a slightly long one; result 0 chunks <50 chars (max 317), and the formerly-hallucinated sentence now reads in 1.5s. Confidence: 0.8
- TTS chunking must keep consecutive sentences with identical openings (parallel structure, e.g. "một số đứa ở lại lớp bảy." / "một số đứa chuyển sang trường khác.") in separate chunks — adjacent sentences that start with the same phrase make the TTS model get stuck repeating; the split must be enforced at the sentence-accumulation stage of `smart_chunk` (detect same first-2-words of consecutive sentences and force a chunk break), not at the later small-chunk merge stage, where the parallel sentences are already joined and undetectable. Confidence: 0.65
- Prefers environment-adapted verification tools with minimal third-party dependencies — when the project venv is broken or system packages are unavailable, the assistant should proactively adapt: use stdlib-only solutions (e.g. wave/struct instead of soundfile), switch commands from hardcoded `.venv\Scripts\python.exe` to system `python`, and run tests via a plain unit harness rather than requiring pytest or other external tools; these environment-driven adaptations were explicitly accepted as part of the approved work rather than being seen as limitations. Confidence: 0.8
- Wants the assistant to execute tasks directly instead of just explaining how — asked "bạn làm luôn giúp tôi được không" (can you just do it for me?) after being given terminal instructions for `cmdc update`, then immediately confirmed "tôi đã chuyển chế độ rồi" (I've switched modes already) to enable the assistant to run commands; expects hands-on execution of commands/actions rather than step-by-step guidance, and is impatient with being redirected to run things themselves — when the assistant finished a fix by telling the user "Bạn chỉ cần chạy: dotnet build ..." (you just need to run this), the user immediately pushed execution back with "donet build cho tôi" (build it for me), expecting the assistant to run verification commands itself rather than instructing the user to. The hands-on mandate extends to installing tools on the user's machine: for lazygit the assistant autonomously ran the `where lazygit` existence check, installed it via `winget install lazygit -e --accept-source-agreements --accept-package-agreements` (180s timeout), located the actual binary under WinGet Packages when the shell alias wasn't yet active, and verified availability — a plain question like "mở lazygit như thế nào" (how do I open it?) is treated as a cue to install, verify, and hand back working launch instructions, not to walk the user through installing it themselves. Confidence: 0.9
- Authorizes system-level environment repairs after root-cause diagnosis — agreed to let the assistant download and run Microsoft's WebView2 Runtime installer/reinstaller (and attempt uninstall) as the recommended fix for the blank EPUB preview, choosing the automated repair option over manual instructions; expects the assistant to first confirm the root cause and obtain consent before touching system-level components. Confidence: 0.75
- When a build fails because the app's own processes are still running and locking the output files, the assistant should kill those processes itself and retry the build, rather than reporting the failure or asking the user to close the app — in the EPUB preview refactor, the assistant ran `taskkill /F /IM TranslateBook.exe` (3 instances) and rebuilt successfully with 0 errors/0 warnings without user intervention. Freshly reconfirmed when the glass-button build was blocked by the running app (PID 36752) locking the exe: the assistant ran `taskkill /PID 36752 /F` and rebuilt with 0 errors/0 warnings, with no user intervention. Confidence: 0.8
- While the user's own desktop app is actively running a live pipeline (a real-time translation run the user started by clicking "Dịch Toàn bộ"), the assistant must NOT edit/rebuild the code that process is executing and must not launch a parallel pipeline against the same book/data — doing so would collide on the working data and could break the running run; when asked mid-run to analyze the pipeline's reliability, the expected behavior is to diagnose from the user's pasted log + read-only inspection of the relevant source and config files (e.g. MainViewModel.cs pipeline orchestration, ConfigService/config.json, the ApiTranslationService network layer), then present the proposed fixes (e.g. wrap each chunk-translation task in catch so one failed chunk doesn't abort the whole pipeline, add an automatic second pass for failed chunks, only merge at 100%, keep progress instead of clean-slate on failure) and stop to ask the user whether to pause/cancel their running run before any code change is applied. Confidence: 0.75
- The decisive troubleshooting lever when the user's long-running "Dịch Toàn bộ" runs keep dying on API/provider errors is to switch to a more reliable provider/model (e.g. from free models proxied through commandcode.ai — which chronically return 503/524/1102 overloads — to a direct provider whose API key is already stored in the app's config, such as direct Google Gemini), not merely to harden retries on the flaky proxy; before proposing this, verify what providers/keys actually exist in the app's saved config (`~/.translate_book/config.json`: Providers, ActiveProvider, keys) and confirm from the live log which provider/model the failing run is actually using, then give the user exact settings-UI navigation to switch and a plan to re-run. Confidence: 0.8
- User-provided screenshots are authoritative ground truth for troubleshooting, even when they show the user's earlier attempt misapplied — if the assistant told them how to switch providers and they reply "đã làm rồi mà" (already did that) plus a screenshot, the screenshot's actual visible state (which provider profile/model is selected) decides whether the change was truly made, and the assistant describes the exact settings-UI steps remaining (dropdown to the right built-in preset such as "♊ Gemini Trực tiếp (Google AI Studio)", model field, then Quét / Lưu cấu hình / Kiểm tra kết nối) rather than taking the claim at face value or blaming the user; misconfiguration like keeping the commandcode proxy profile while believing they switched to direct Gemini is treated as a natural UI-navigation gap, not negligence. Confidence: 0.6
- Prefers machine-parseable status markers in agent outputs (e.g. `FINAL_STATUS: APPROVED` / `FINAL_STATUS: NEEDS_CHANGES` / `FINAL_STATUS: BLOCKED`) rather than relying on natural-language string searches for control flow — proposed this specifically because searching for a word like "APPROVED" inside review feedback risks false positives when the word also appears in feedback text; a dedicated deterministic marker is required for reliable orchestration, with `BLOCKED` signalling any state where the agent cannot proceed (errors, timeouts, missing output, or conflicting pre-existing changes). Confidence: 0.85
- Enforces scope adherence during review — the executor agent must only create/modify files within the approved plan, and the analyzer's review must verify this (e.g. via git diff / out-of-scope file detection) before APPROVING, preventing the implementation from silently expanding beyond the agreed scope. Confidence: 0.8
- Tests end-to-end workflows in isolated/scratchpad environments with cleanup rather than polluting the main repository working tree — proposed using a temporary fixture/worktree for verification runs and cleaning up afterward so the live repo stays clean and rollback is easy. Reconfirmed when the user asked to "test thử hoạt động" (test whether it works) after the glossary-master upgrade: the assistant ran a fake-book merge against the LIVE master.csv (observing the auto-split into master_001), then restored the data file to its exact pre-test state (re-merged shards back into master.csv, back to 346 rows, deleted master_001 + the test book CSV and folder) and re-ran the full state checks (--check, --check-dup, verify_memory, git status) before reporting — even a live-data test must leave the data and working tree exactly as found, not merely a scratch directory test. Confidence: 0.85
- Tests individual sub-agents in isolation (via the correct delegation/task-tool API, e.g. `agent(subagent_type="analyzer", ...)`) before running the full end-to-end orchestration loop, to confirm each agent behaves correctly before integration. Confidence: 0.7
- Orchestrator must handle agent failures gracefully — if an agent errors, times out, or returns output missing the required `FINAL_STATUS` marker, do not proceed to subsequent steps; instead return `FINAL_STATUS: BLOCKED` with the failure reason, preventing cascading failures from incomplete or corrupted agent output. Confidence: 0.85
- Executor must stop before editing and return `FINAL_STATUS: BLOCKED` when it detects pre-existing uncommitted changes (e.g. from `git status --short`) that overlap with files it needs to modify, rather than risking overwriting or clobbering in-progress user work. Confidence: 0.85
- Prefers safe, non-destructive cleanup (delete only test-created files or restore from file snapshots) over `git checkout`, which can destroy in-progress uncommitted user changes that haven't been backed up elsewhere. Confidence: 0.8
- When making code/UI changes, only needs a build verification (`dotnet build`) — does NOT want the assistant to run the app (`dotnet run`) to test; the user will launch and check the UI themselves. Confidence: 0.9
- Explicitly delegating a UI check to the assistant overrides the "user checks the UI themselves" convention — a terse "kiểm tra cho tôi giao diện nút Dịch toàn bộ, Ra soát, đọc sách" (check the UI of the Translate-all / Review / Read buttons for me) after a UI change means the assistant must actually RUN the app and verify the RENDERED appearance, not just confirm the build: kill any stale running instance, rebuild, launch, screenshot the window, analyze with vision plus pixel-level border checks (e.g. confirming the mirror-glass gradient spec: brighter at top-left/bottom-right corners, dimmer mid-edge — not trusting a vague visual description alone), prepare the environment so the target elements are actually visible (e.g. temporarily seeding an input file when the input folder is empty, then restoring it), and end with a per-button verdict table plus full cleanup of temp scripts/screenshots/app process. Confidence: 0.7
- Prefers a phased MVP-first approach for multi-agent orchestration — build a non-looping single-pass workflow (analyze → execute → review) first, prove core mechanics (inter-agent output passing and orchestrator-mediated git-diff handoff) work end-to-end, THEN add the iterative fix loop (Phase 2) only after the fundamentals are verified; rejects jumping straight to full complexity. Confidence: 0.85
- Has the orchestrator (main session AI with full tools) perform git operations and inject the output into a read-only agent's prompt context when that agent needs to verify something outside its stripped tool set (e.g. the analyzer has no shell_command but must review git diff) — rather than grant the read-only agent mutating tools. Confidence: 0.85
- Prefers natural-language, step-by-step instructions in slash-command markdown over pseudo-code with `$VARIABLE` references (e.g. `$PLAN`/`$RESULT`) — any `- Wants visual/interactive UI layer over pure CLI workflows — asked for "giao diện đẹp" (nice interface) and the assistant built a Rich TUI app for managing the project; values having a polished interface for tasks like status checking, audiobook creation, and git ops rather than remembering raw commands. Confidence: 0.8
- Prefers desktop GUI apps over web apps (Streamlit) or pure TUI — originally accepted PyQt6, then explicitly migrated to C# WPF for Windows-native performance; still values TUI for quick terminal tools but wants a full desktop app for primary workflows. Confidence: 0.8
- Prefers "Liquid Glass" aesthetic for UI — explicitly requested "giao diện UI đẹp phong cách Liquid Glass màu sắc tươi mới" (beautiful UI with Liquid Glass style, fresh colors); likes glass morphism, soft gradients, semi-transparent elements, and vibrant color palettes. Confidence: 0.9
- Prefers simple launch methods (e.g., .bat files for double-click) over remembering terminal commands — asked "tôi mở kiểu gì nhỉ" (how do I open it?) and the assistant proactively created an app.bat for one-click launch; indicates comfort with UI tools but not with remembering CLI invocations. Confidence: 0.8
- Wants additive-only changes when building on existing stable architectures — explicitly said "đặc biệt ko phá vỡ cấu trúc logic đang rất ổn của dự án" (especially don't break the already-stable project structure); expects new modules/features to be added as entirely new files/directories without modifying existing pipeline scripts; values architectural stability highly. Confidence: 0.9
- Requires backward compatibility with existing data formats and completed work when refactoring — explicitly scoped changes so existing progress JSON formats are not altered ("Không thay đổi format progress JSON hiện có để bảo đảm tương thích với merge_chunks.py và các sách đã hoàn tất"), old progress without metadata still recognized on resume, and merged/completed books remain valid; new coordination layers (manifest, cache fingerprints) must be additive guards, never a new source of truth that breaks old data. Confidence: 0.75
- Wants API key integration for automated translation — explicitly requested adding API key support (Gemini, DeepSeek, OpenAI-compatible) so the assistant can call APIs directly for translation instead of relying solely on the chatbot agent; values the convenience of API-driven pipelines. Confidence: 0.85
- Wants real-time progress/status display in UI — explicitly asked for "khu hiện trạng thái chạy nữa nhìn cho nó nguy hiểm" (status area that looks impressive); wants visible progress tracking with animated indicators showing translation status, not just static info. Confidence: 0.85
- Prefers to keep stable/configured settings unchanged when they work — when told the prompts directory is functional and not broken, said "giữ nguyên thư mục này đi không cần đổi đâu" (keep it, no need to change); avoids unnecessary changes to things that already work. Confidence: 0.8
- Prefers careful verification and merging over outright deletion of legacy code — explicitly said "kiểm tra kĩ nhé xem nếu mà cần thiết thì gộp vào nhé" (check carefully, if needed merge it in) before authorizing removal of deprecated scripts/api/; wants thorough analysis of whether legacy code has useful functionality worth consolidating before discarding. Confidence: 0.8
- Wants UI to show complete picture of all items in the pipeline, including pending/input — expects the book list to display both already-processed output books AND untranslated input files; doesn't want incomplete views that only show one stage of the workflow. Confidence: 0.85
- Prefers tabbed/separated UI over mixed lists when items have distinct states — finds combined lists "khó kiểm soát" (hard to control); explicitly asked to split book list into Input tab and Output tab rather than showing everything in one scrollable list; values clear categorical separation in UI. Confidence: 0.85
- Wants todo lists cleared promptly after task completion — finds lingering/stale todos visually annoying ("cứ hiện nhìn khó chịu"), wants them cleaned up immediately rather than left on screen. Confidence: 0.85
- Prefers Windows-native UI frameworks (C# WPF) over cross-platform Python frameworks (PyQt6) when the project is Windows-only — explicitly chose to migrate the entire desktop app from PyQt6 to C# WPF for native look, lower memory (~50MB vs ~200MB), faster startup, and genuine Liquid Glass animations; willing to accept slightly slower dev speed for better runtime quality. Confidence: 0.85
- Prefers clean architecture separation by language — explicitly chose C# for UI layer only while keeping Python for pipeline/scripts; expects the best tool for each job rather than a single-language monolith; the C# app calls Python via subprocess for pipeline operations. Confidence: 0.85
- The primary/current translation workflow runs through the AI Agent directly, not API calls — explicitly said "bình thường sẽ dùng AI Agent" (normally I use the AI Agent), "tôi không dùng API đâu" (I don't use API), and reaffirmed "chức năng API sẽ phát triển sau" (API features will be developed later); API is only planned later to serve the desktop software, so performance/optimization work should target the agent loop (fewer round trips, batching chunks, less re-reading of files) rather than HTTP concurrency/retry tuning. Confidence: 0.95
- Bounds iterative review loops to a maximum iteration count (e.g., 2 per session) to prevent infinite feedback loops — explicitly requested capping review/fix cycles so an analyzer → executor → analyzer review cycle cannot loop forever, treating the bound as a safety guard rather than a loose guideline; the user also wants the counting model precise and unambiguous: 1 initial implementation + up to N fix iterations counted separately (not "N total executions"), e.g. initial implement + max 2 fixes = 3 total executor calls, and the user rejected vague "max 2 iterations" phrasing that could mean 2 total or 2 fixes. Confidence: 0.9
- Prefers multi-model specialization for agent roles — wants different models linked together for different agent functions (e.g., Claude Opus for deep analysis/review, GPT/Codex for coding/execution) rather than using the same model across all agents. Confidence: 0.85
- Prefers role separation between a read-only analyzer agent and a write-capable executor agent — the analyzer only analyzes, creates implementation plans, and reviews output (never writes files), while the executor only implements and writes code; this clean separation prevents the reviewing agent from directly altering implementation, and the user enforces it at the tool level by stripping any potentially-mutating tools (e.g. shell_command) from the analyzer so read-only is guaranteed, not just conventional. Confidence: 0.85
- Orchestrates multi-agent workflows via slash commands with `agent: build` frontmatter — the orchestrator slash command runs on the main session and synchronously calls specialized sub-agents via `agent()` calls, passing structured context between them through the orchestration logic. Confidence: 0.8
- Requires explicit slash-command activation for multi-agent workflows (e.g., `/dual-Agent <task>`) — does NOT auto-trigger based on automatic detection of complex tasks; typing the task alone runs in normal single-agent mode. The dual-agent review loop only runs when the user explicitly invokes the slash command. Confidence: 0.95
- Verifies that planned slash commands are actually registered in the Command Code runtime by inspecting the command directory (file presence + directory listings to confirm autocomplete availability), not just by reading the plan document; asks for repeated re-checks to confirm current runtime state before relying on a command. Confidence: 0.85
- Prefers fully automatic, hands-off multi-agent orchestration — wants a single command to trigger the entire agent pipeline (analyze → execute → review → loop) with zero manual intervention or step-by-step agent invocation; the user explicitly checked whether agents "work automatically together" or require manual management and expects confirmation that the orchestrator handles all iteration counting, context forwarding, and agent calls on its own. Confidence: 0.9
- Prioritizes correctness/completeness over raw speed when parallelizing translation — worried whether parallel translation would "dễ bị sót hay bị loạn" (easily miss or scramble content); expects any parallel scheme to guard against content loss/duplication, style/terminology drift, broken context, and file overwrites, e.g. parallelize by independent chapter groups with non-overlapping per-agent chunk manifests, a fixed shared glossary/rules, per-chunk writes, and completeness/order/terminology verification before merge; prefers one agent handling a full chapter rather than splitting mid-chapter. Confidence: 0.65
- Applies the same safety-first, conservative approach to audiobook/TTS optimization as to translation — accepts bounded parallelism (e.g. 2 workers by chapter) only with sufficient RAM/CPU, and only after benchmarking; preserves checkpointing to avoid re-generating finished audio, and prioritizes keeping existing stable settings (temperature=0.3, top_k=10) over speed gains that could hurt voice quality. A proposal explicitly deferring parallel TTS until after measuring RAM, RTF, and real voice quality was approved as-is. Confidence: 0.85
- Prefers environment-adapted verification tools with minimal third-party dependencies — when the project venv is broken or system packages are unavailable, the assistant should proactively adapt: use stdlib-only solutions (e.g. wave/struct instead of soundfile), switch commands from hardcoded `.venv\Scripts\python.exe` to system `python`, and run tests via a plain unit harness rather than requiring pytest or other external tools; these environment-driven adaptations were explicitly accepted as part of the approved work rather than being seen as limitations. Confidence: 0.8
- Wants the assistant to execute tasks directly instead of just explaining how — asked "bạn làm luôn giúp tôi được không" (can you just do it for me?) after being given terminal instructions for `cmdc update`, then immediately confirmed "tôi đã chuyển chế độ rồi" (I've switched modes already) to enable the assistant to run commands; expects hands-on execution of commands/actions rather than step-by-step guidance, and is impatient with being redirected to run things themselves. Confidence: 0.75
- Prefers machine-parseable status markers in agent outputs (e.g. `FINAL_STATUS: APPROVED` / `FINAL_STATUS: NEEDS_CHANGES` / `FINAL_STATUS: BLOCKED`) rather than relying on natural-language string searches for control flow — proposed this specifically because searching for a word like "APPROVED" inside review feedback risks false positives when the word also appears in feedback text; a dedicated deterministic marker is required for reliable orchestration, with `BLOCKED` signalling any state where the agent cannot proceed (errors, timeouts, missing output, or conflicting pre-existing changes). Confidence: 0.85
- Enforces scope adherence during review — the executor agent must only create/modify files within the approved plan, and the analyzer's review must verify this (e.g. via git diff / out-of-scope file detection) before APPROVING, preventing the implementation from silently expanding beyond the agreed scope. Confidence: 0.8
- Tests end-to-end workflows in isolated/scratchpad environments with cleanup rather than polluting the main repository working tree — proposed using a temporary fixture/worktree for verification runs and cleaning up afterward so the live repo stays clean and rollback is easy. Confidence: 0.8
- Tests individual sub-agents in isolation (via the correct delegation/task-tool API, e.g. `agent(subagent_type="analyzer", ...)`) before running the full end-to-end orchestration loop, to confirm each agent behaves correctly before integration. Confidence: 0.7
- Orchestrator must handle agent failures gracefully — if an agent errors, times out, or returns output missing the required `FINAL_STATUS` marker, do not proceed to subsequent steps; instead return `FINAL_STATUS: BLOCKED` with the failure reason, preventing cascading failures from incomplete or corrupted agent output. Confidence: 0.85
- Executor must stop before editing and return `FINAL_STATUS: BLOCKED` when it detects pre-existing uncommitted changes (e.g. from `git status --short`) that overlap with files it needs to modify, rather than risking overwriting or clobbering in-progress user work. Confidence: 0.85
 notation should be described as descriptive symbols held in the orchestrator's context memory, not runtime variables or shell expansions. Confidence: 0.85
- Requires the executor agent to emit a structured, machine-verifiable output block (FINAL_STATUS marker, full file list, diff summary, and test command + results) so review is grounded in concrete data rather than the executor's self-reported narrative. Confidence: 0.8
- Explicitly declares the working directory (project root) for all git operations and agent context in orchestration commands, rather than leaving cwd implicit or ambiguous. Confidence: 0.75
- Orchestrator must capture a baseline git snapshot BEFORE the executor runs (`git status` for untracked files, content hashes of relevant files, staged/unstaged diff); `NEW_CHANGED_FILES` is then computed by diffing before/after, and the reviewer only evaluates workflow-produced changes. `git diff` alone is insufficient because it does not surface untracked (`??`) files, which must also be included in the review set. Confidence: 0.85
- Limits payload/context size passed to sub-agents (PLAN, RESULT, REVIEW, git diff output, logs) with concrete per-field thresholds (e.g. PLAN ≤ 24 000 chars, RESULT ≤ 20 000 chars, REVIEW ≤ 16 000 chars, diff/log ≤ 30 000 chars, total prompt ≤ 70 000 chars) rather than abstract "configurable threshold" phrasing; when truncating preserves in priority order: success criteria → file scope → test errors → relevant diff/feedback, trimming logs, repeated output and out-of-scope diff first, to prevent token/context overflow even within the bounded review limit. Prefers concrete, explicitly-defined numeric limits over vague "configured threshold" abstractions. Confidence: 0.85
- Executor agent must enforce shell-command safety restrictions: forbids destructive git ops (git reset, git checkout, git clean, git restore), bulk file deletion (Remove-Item, del, rmdir), and out-of-scope mutating operations (commit, push, branch creation, history rewrite, permission changes, dependency installs, system config changes); returns FINAL_STATUS: BLOCKED instead of executing any command that risks clobbering files outside the approved plan scope. Confidence: 0.9
- Prefers reviewing proven patterns from related/reference projects and explicitly assessing which parts are applicable before changing the current architecture, rather than blindly copying or reinventing the approach. Confidence: 0.85
- Prefers task-tracking artifacts to have a bounded lifecycle so the repository does not accumulate unmanageable numbers of files; favors retention, archival, or automatic cleanup rather than indefinite manual deletion, while preserving useful audit history. Reconfirmed when the user asked whether `docs/session_log.md` auto-deletes after a number of days ("phần session_log này thì có tự động xoá sau bao nhiêu ngày ko chứ ko nó sẽ rất là dài" — does this auto-delete after some days, else it gets very long), worried about the log growing indefinitely; expects log/session artifacts to be automatically rotated/archived (by size or time, e.g. keeping only recent entries in the main file and archiving older ones) rather than appended forever with no mechanism. Confidence: 0.8
- Wants session-lifecycle slash commands (in the agent runtime, i.e. Command Code's `.commandcode/commands/`) that automate memory and repo bookkeeping around each session — after choosing the rotate-script option for the session log, tersely ordered "C và add /done với /start vào trong commandcode cho tôi nhé" (C, and add /done and /start into Command Code for me): `/start` reads `docs/STATE.md` + the last `session_log.md` entries and summarizes project state/next steps before work begins; `/done` is the mandatory closing step that appends a memory entry, runs the log-rotation script when the log exceeds the size threshold, runs `manage_input.py` to move the source file into the correct input/ status folder, and proposes a commit for the user to approve. Confidence: 0.7
- Prefers sequential workflow execution: finish and verify the currently approved change before moving on to analyze or implement a separate reference-project improvement, avoiding scope expansion or context switching mid-task. Confidence: 0.85
- Prefers deterministic, least-privilege sub-agent permissions over interactive prompts or retries: use non-interactive `dont-ask` mode with explicit allowlists, keep the analyzer read-only, keep the executor limited to file read/write/edit tools without shell access, and let the main orchestrator handle Git and test commands. Confidence: 0.9
- Wants a clear, step-by-step explanation of what happens internally when invoking a slash command, including agent roles, handoffs, review branches, stopping conditions, and final outputs. Confidence: 0.85
- Wants dead/disconnected sub-agent detection even under the "wait indefinitely" policy — asked "nếu Agent 2 bị mất kết nối ko còn hoạt động thì có cách nào để Agent 1 nó biết để trả về kết quả ko" (if the executor loses connection and stops working, is there a way to detect it and return a result); expects a monitoring mechanism that distinguishes "slow but still running" (keep waiting indefinitely) from "dead/disconnected" (return FINAL_STATUS: BLOCKED with the reason immediately), so the no-time-limit wait never becomes an indefinite silent hang; confirmed by explicitly requesting the monitoring rule be added to the orchestration command ("thêm quy tắc đó cho tôi"). Confidence: 0.9
- Characterizes Laguna (the free executor model) as weak/unreliable ("Laguna khá yếu") and therefore requires vigilant, regular/frequent health-check polling of the executor during the indefinite wait — checking often to detect if it has died/stalled, specifically because its weakness makes drops and stalls a real risk; the orchestration must distinguish "slow but still alive" (keep waiting) from "dead/disconnected" (return BLOCKED). Confidence: 0.85
- Keeps the dual-agent orchestration setup synchronized across both Command Code and OpenCode — the same `dual-Agent.md` command (plus the analyzer/executor agent definitions) is maintained in both `.commandcode/` and `.opencode/` directories; every change is mirrored to both versions and verified to have identical non-empty content (e.g. via Compare-Object/diff), so both tools behave the same. This same dual-location sync discipline extends to ALL shared workflow/config docs — when `dich.md` (the translation pipeline rules) is edited, it is copied to both `.commandcode/commands/dich.md` and `.opencode/command/dich.md` in one step and the temporary helper scripts used to make the change are deleted afterward. Confidence: 0.7
- Slice-manifest/checkpoint artifacts of the dual-agent workflow must be stored OUTSIDE the task's file scope (e.g. `working/slice_manifests/<slug>/`), never inside the task directory — the E2E slice test caught manifest JSON files inside `working/e2e_slice/` as a scope-adherence violation (Review 1 NEEDS_CHANGES), and relocating the manifest outside the task dir resolved it; coordination bookkeeping must never count as part of the task's deliverable file set, or the scope-adherence review will (correctly) reject it. Confidence: 0.7
- The orchestrator must report progress to the user periodically during long sub-agent runs (e.g. "Đang làm slice N/X: <tên>", "Slice N/X xong ✅", and which slices remain if interrupted) rather than silently waiting indefinitely — the user approved adding this reporting rule so the fully-automatic workflow stays observable while the executor runs. Confidence: 0.65
- Wants long executor tasks handled via automatic plan slicing with incremental delivery — asked whether there is a method where Agent 1 (analyzer) automatically splits an overly long plan into small pieces handed to Agent 2 (executor) one at a time, yet the final result still matches the original full plan ("Agent 1 sẽ tự động tách Plan việc ra giao từng chút một nhưng cuối cùng vẫn trả về đúng kết quả như một plan to"); expects master plan + per-slice checkpoints + final aggregate review against master success criteria rather than one monolithic handoff. Confidence: 0.7
- Requires strict adherence to the dual-Agent workflow whenever `/dual-Agent` is invoked — even for bug fixes, the orchestrator must run the full sequence (Analyzer analysis/plan → Baseline snapshot → Executor implementation → Analyzer Review) and must NOT bypass the sub-agents by directly editing project files itself; the user explicitly caught the assistant skipping the workflow and hand-fixing a compile error ("hình như bạn ko dùng dual AGent"), and the assistant acknowledged it should follow the steps and route all code changes through the executor on future invocations. Confidence: 0.9
- When the user requests a fully automatic run ("tôi muốn nó tự động hoàn toàn tôi cần kết quả cuối thôi" — I want it fully automatic, I only need the final result), the assistant must resolve operational blockers autonomously instead of stopping to present decision options (e.g. A/B/C choice tables) to the user — including terminating running app processes that lock build output and granting the executor the missing shell access (with deny rules for dangerous git/file commands) — then report only the end result (e.g. FINAL_STATUS: APPROVED with real build evidence). The proposal-first preference applies to design/architecture decisions, not to operational blockers encountered while a workflow is running. Confidence: 0.9
- Prefers descriptive, purpose-revealing file names for project assets over generic numbered names — renamed the music file from "1 lofi.mp3" to "sach_ke_chuyen_lofi.mp3" and informed the assistant "tôi có đổi tên cái music cho nó hợp lý hơn nhé" (I renamed the music to something more sensible), expecting the assistant to pick up the new name and re-run any samples/references against it. Confidence: 0.6
- Wants lessons discovered during E2E testing/review codified back into the persistent workflow command docs (e.g. the manifest-location lesson added as a dedicated "### Vị trí manifest slice" section in `dual-Agent.md`) so future runs are automatically consistent, rather than relying on memory of one-off fixes — approved the assistant's offer to write the discovered rule into the docs with a single "có". Confidence: 0.65
- Prefers targeted, minimal-scope regeneration when a source-data fix affects only part of a completed audiobook — after `vi.md` was repaired (chunk 1 mojibake) and the user tersely ordered "vi.md sửa ok chưa thì chạy lại cho tôi đi" (if vi.md is fixed, re-run it for me), the user chose to regenerate only the affected chapters (ch 1–2 via `--chapter 1 2 --force`) instead of re-running all 50 chapters, keeping the 48 unaffected MP3s as-is; expects the assistant to first verify the source file is actually clean (0 mojibake) and then scope expensive re-runs to exactly what the change invalidated rather than regenerating unchanged work. Confidence: 0.65
- Knows that `audiobook_long.py` will NOT regenerate audio on a plain re-run after the source md changes — a changed source fingerprint is insufficient when the progress JSON already marks all chapters completed (the script exits in ~2 seconds with exit 0 and touches nothing); forcing regeneration requires `--force`, which can be scoped to affected chapters with `--chapter N M` to reuse cached/unchanged audio for the rest, and `--force` resets the progress JSON to only the regenerated chapters (subsequent runs then need `--force` or reconcile to proceed). Confidence: 0.7
- For cost-optimized dual-agent workflows, prefers a 3-tier model specialization: Luna reserved for the planning/analysis step only (used once, the single most capable reasoning task), free Laguna for code execution, and deepseek flash (near-zero cost ~$0.01) for the independent review step — this makes dual-agent cheaper than running single-agent on Luna (~$2.75 vs ~$3.65) while preserving a separate reviewer agent for objective validation. Confidence: 0.65
- Prefers empirical benchmarking with real measurements over theoretical estimation when comparing agent approaches — explicitly asked the assistant to "benchmark một task nhỏ" (run an actual benchmark on a small task) and extract measured token/cost data from CLI session transcripts (`usage` fields in JSONL files), rather than relying on the theoretically estimated token counts previously provided; wants real, end-to-end measured results (including running both single-agent and dual-agent on identical tasks via separate CLI sessions and comparing the actual transcript deltas) before making judgments about resource usage trade-offs. Freshly extended to pipeline cost questions: when asked "dịch một cuốn sách mất khoảng bao nhiêu tokken" (roughly how many tokens does translating a book take?), the accepted answer was derived from the project's OWN real data — measuring actual character counts in the 9 translated books' progress JSONs (~90–130K source chars → ~300–450K translated chars, ≈400–600K tokens/book including ~1.5–2x pipeline overhead, ~55 chunks avg) and per-model cost at the cheapest rates — rather than a generic rule-of-thumb; token/cost figures for their pipeline should be measured from the project's actual outputs, not estimated from theory. Confidence: 0.92
- When asked a factual question about the project's own pipeline (e.g. token consumption per book), expects the assistant to ground the answer in the project's real data files — STATE.md, session_log.md, and the actual per-book progress JSONs in `working/progress` (measuring source/translated character counts, chunk counts) — rather than answering from memory or generic estimates; the assistant's verified-data approach (reading STATE.md + session_log + running a measurement script over all translated books' chunk JSONs) was accepted as the correct way to answer the token-estimate question. Confidence: 0.6
- Prefers explicit `subagent_type=` specification for every subagent invocation in orchestration prompts (including the reviewer, not only the analyzer), matching across all workflow steps so agent dispatch is unambiguous and consistent rather than relying on implicit/default invocation. Confidence: 0.7
- Delegates whole-book/long-running batch generation to the assistant and expects it to run in the background — "làm cho tôi cả chương xem nào" (do the whole chapter/book) means kick off the full 12-chapter pipeline in the background and monitor progress periodically, not run a partial/demo slice; the assistant tracks progress via the progress JSON, inspects running shell tasks, and reports a status summary with ETA rather than blocking the session on the multi-hour run. Reconfirmed verbatim with "hãy làm cho tôi full cả cuốn nhé" (please make the full book for me) right after a single chapter was validated — the user authorizes the entire book to be generated (resuming only the missing chapters, keeping completed ones) even while they plan to keep listening/checking in parallel, expecting a background run plus a status report (PID, log path, how to monitor progress). Reconfirmed again with the terse "làm cả quyền đi bạn" (do the whole book, please) right after the sample chapter was delivered — one short sentence is enough to authorize the full 42-chapter run; the assistant is expected to check progress JSON for already-completed chapters and resume only the missing ones without `--force`. Reconfirmed once more when the user moved on to a brand-new book with a single terse sentence "làm tiếp cuốn này cho tôi nhé Rung Na-uy - Haruki Murakami" (continue, do this next book for me) — one line sufficed to authorize the entire next-book pipeline autonomously (locate the input file, detect format EPUB vs PDF and language Vietnamese-vs-needs-translation, pick the appropriate extraction path, build vi.md, validate chapter detection/chunking, run a sample, then launch the full background run), with no clarifying questions about the input format or TTS parameters (the established temp 0.3 / top_k 10 / rep_penalty 1.5 / top_p 0.95 were reused without re-asking). Confidence: 0.95
- Long-running background jobs (e.g. whole-book TTS pipelines) must be launched as detached, hidden processes via `Start-Process -WindowStyle Hidden` with stdout/stderr redirected to a log file (e.g. `working/audiobook_run.log`), never as session-bound background shell tasks — background tasks tied to the shell session die when the session ends (the 12-chapter pipeline stopped after 2/12 chapters and the visible PowerShell window closed with it), whereas a detached `Start-Process` survives the session and creates no console window; progress is then verified via the progress JSON and the redirected log after the session is gone. When a long background job must be monitored live, checks the actual python process state (CPU-seconds climbing, RAM) rather than trusting empty log output — a script with `sys.stdout.reconfigure(encoding="utf-8")` but no flush can print nothing for many minutes when its stdout is a pipe (block-buffered), so an empty log does NOT mean the job stalled; process CPU/RAM growth and newly-written chunk-cache files are the reliable liveness signals, with final completion confirmed via exit code, output files, and progress JSON. Confidence: 0.85
- Prefers removing overly-restrictive safety rules (e.g. deny rules) when they block routine authorized operations, rather than adding per-operation temporary allow exceptions — when a deny rule like `Shell(git commit*)` prevents the user from committing after already approving a commit message, the user says "sửa nó lên" (fix it) meaning remove the rule, accepting the trade-off of less protection for smoother workflow over adding narrow one-time allow rules for each command; reconfirmed when offered to open `git reset`/`git restore` too, replying "mở luôn cho tiện" (just open them for convenience) with no further discussion. Confidence: 0.85
- Expects workflow-blocking permission/config fixes to be made permanent in the repo and verified durable for future sessions, and understands permission edits need a reload before retrying — after the assistant removed deny rules from `.commandcode/settings.json` to unblock `git commit`/`git push`, the user asked "lần sau có push được không đấy" (will pushing work next time?), expecting the assistant to verify the settings change is committed to the repository (e.g. `git show HEAD:.commandcode/settings.json`), not merely saved locally; the assistant also discovered permission-config edits do not take effect in the running session until the user reloads, so the user typed `/permissions` then `/reload` and reported "bạn xem được chưa tôi reload rồi đấy" (can you check now, I've reloaded) — expecting the assistant to immediately retry the previously-blocked git operation after each reported reload. Confidence: 0.6
- Finds purely text-based, plain card UI "hơi phẳng" (a bit flat) and proactively solicits design-improvement proposals for it ("bạn có đề xuất gì về giao diện không") — expects the assistant to inspect the current XAML plus the backing model, then propose concrete, prioritized visual enhancements that reuse existing model fields before implementing anything. When the proposal is offered, the user approves the full set at once ("ok đó bạn làm tất cả cho tôi xem nhé"), and the approved design language for book cards is: circular avatar with the book's initial letter, colored header with title + status, status badge, stat tiles (icon + value + label), progress bar with percentage, and hover lift (translate + stronger shadow). Confidence: 0.75
- Judges UI work on visual polish as well as functionality — after a layout change works, feedback like "cũng khá được nhưng nhìn nó vẫn hơi thô" (it's okay but still looks a bit rough) means functional correctness alone is not the bar: the user expects the assistant to run an autonomous visual-refinement pass (alignment, spacing, integration with the surrounding UI, removing floating/detached elements) and iterate on the look until it feels smooth, not to leave a working-but-rough layout as-is. Confidence: 0.7
- Cares about smooth, jitter-free UI interactions — reported hover over a card's progress bar causing continuous "giật giật" (jitter); the root cause was the hover translate transform moving the card so the cursor left the hit area and re-entered it in an infinite flicker loop, so hover effects must NOT move/translate the element near edges (and must not cause re-layout); hover feedback should use non-movement cues (stronger shadow, border/opacity) instead. Confidence: 0.75
- Wants animated transitions for UI state changes like tab switching, not abrupt show/hide — explicitly requested "thêm hiệu ứng chuyển tab input output" (add tab transition effects); approved the WPF implementation using `DoubleAnimation` fade (opacity 0→1) plus a slight upward slide (~12px→0, ~180ms) with `CubicEase.EaseOut` easing for a natural, non-jarring feel. Confidence: 0.75
- When adding new UI controls/panels, expects them to reuse the app's existing visual patterns rather than introducing novel styles — explicitly asked for the realtime-log collapse/expand toggle to look like the existing hamburger (☰) button ("có nút bấm bấm sang hoặc thu về giống nút ba gạch [Image #1] trong khung đỏ"); new controls should mirror established conventions already present in the app (e.g. reuse existing ViewModel toggle commands and collapsed-bar patterns). Freshly reconfirmed for EXISTING controls too: a terse request to add the "khung viền tráng gương" (mirror glass border) to buttons like "Dịch toàn bộ" and "Sửa chữa & Rà soát" ("cũng thêm cho tôi cái khung viền tráng gương như các Nút bấm khác") means every major primary/secondary action button across all pages (Books/Audio/Api) must be brought into the app's established glass-button visual language — UI consistency is the goal, so no action button should be left on a default style when a glass variant exists. Freshly reconfirmed with the terse order "Đọc giao diện desktop cho tôi các nút bấm tạo thành viền sáng kiểu gương khớp phù hợp giao diện Liqui Glass đang build" (read the desktop UI and make the buttons have bright mirror-style borders matching the Liquid Glass theme) — the consistency scope covers ALL buttons across every view (including icon/delete/cancel/header-refresh buttons that still fall back to default WPF/WPFUI styles), and the request is answered with a full-codebase XAML audit (grep every view incl. preview windows) categorizing each button as already-glass vs default, with a per-button proposed-fix table (which glass style to apply) presented for approval before any edit. Freshly extended beyond buttons to ALL control types: when the user spotted the Realtime Log progress bar still using the default flat/square style, the terse order "cái thanh process ở chỗ Reltimelog ấy thì mình nên cho nó có hiệu ứng như gương để đồng nhất với giao diện tổng thế, và bo tròn 2 đầu cho đẹp" (the process bar in the Realtime Log should get a mirror effect to match the overall UI, with rounded pill ends) — every control in an otherwise-glass UI (progress bars included) must be brought into the glass design language, not just buttons: mirror/highlight overlay on the indicator, gradient glass border, and pill-style CornerRadius on both track and indicator; the request is answered by upgrading the shared GlassProgressBar style itself (so all progress bars benefit) and switching the inline unstyled usage to that style. Confidence: 0.9
- The Liquid Glass restyle follows an established per-button-type style mapping in the desktop app: icon-only buttons → `GlassIconButton`; labeled action buttons → `InteractiveButton` (primary actions → `InteractiveButtonPrimary`); plain text buttons → `GlassPlainButton`; collapse toggles → `GlassCollapseButton`; and buttons needing extra triggers (e.g. visibility bound to `IsBusy`) set `BasedOn` on the glass style instead of the default `{x:Type ui:Button}`. Destructive/delete buttons keep their red foreground (`#FFEF5350`) even under the glass style so the danger signal stays visible — the user approved a batch conversion of all 7 remaining default-style buttons (header refresh, open-folder, test-translate, deletes, cancel buttons in BooksPage + AudioPage) under exactly this mapping and expected it applied without further questions. Confidence: 0.7

- When styling a WPF-UI `ui:Button` with a custom standard-WPF `Button` style/template, the `Icon` property of `ui:Button` is NOT rendered — a template exposing only `ContentPresenter` (e.g. `GlassPlainButton`) discards the icon entirely, leaving an empty bordered button frame. The fix/convention is to place the `SymbolIcon` directly inside the button's `Content` (setting any needed `Foreground`, e.g. red `#FFEF5350` for delete) instead of relying on the `Icon` property; when the assistant reports a "button disappeared" (the trash/folder icons in the output/input cards showed only as empty frames), this `ui:Button` `Icon`-vs-`Content` interaction with custom templates is the likely root cause and must be checked before assuming the control was deleted. Confidence: 0.8
- Collapsed side panels must collapse completely into the screen edge, not shrink to a narrow full-height strip — the user rejected the earlier 40px-wide full-height collapsed bar for the Realtime Log ("cái phím 3 gạch đó nó ko có thu dọc vào tôi cần nó thu dọc vào") and expects the panel to disappear entirely (Visibility=Collapsed) when toggled off, leaving only a small compact floating reopen button at the edge; collapsed UI should take no space at all rather than leaving a leftover vertical bar. Confidence: 0.8
- When a floating UI element looks out of place, prefers replacing it with a slim docked edge element that visually integrates with the rest of the layout — a floating ☰ reopen button hovering beside the toolbar looked like a layout bug to the user; the accepted design was a 28px vertical tab strip flush against the right edge, same height as the content, using the app's existing background/border brushes, with the ☰ button centered at the top. Confidence: 0.7
- Prefers simple, standard ASCII directional arrows (`<` / `>`) over a hamburger icon (☰) or fancy Unicode chevrons (⟨ / ⟩) for collapse/expand toggle buttons — first asked for "hình > và < nhìn cho chuyên nghiệp" (add the > and < shapes to look more professional), then explicitly rejected the Unicode glyphs ⟨/⟩ as looking strange and unlike the plain signs ("logic tý đi" — be a bit logical). The arrow must follow standard toggle convention and always indicate the UPCOMING action, not the current state: when the log panel is expanded the button shows `<` (clicking collapses it), when collapsed the button shows `>` (clicking expands it) — the user explicitly corrected the assistant's reversed logic with "nó bấm vào thì nó sổ ra cái log thì nút đó phải hiện là '<' còn lúc chưa sổ ra thì hiện '>'" (when clicked the log expands, so the button must show `<`; while not expanded, show `>`); applies to both buttons while keeping the same toggle command. Confidence: 0.9
- When regenerating audiobooks with a new pipeline configuration, prefers to run/validate a single chapter first (chapter 1) before committing to the full multi-chapter batch — terse request "tôi cần chạy 1 chương đầu đã" (I need to run the first chapter first) right after a whole-book background run was started; expects the assistant to stop the full run and scope execution to chapter 1 only (e.g. `--chapter 1`) so the chapter can be verified/listened to before the long full-book run. Confidence: 0.65
- Wants UI upgrade recommendations grounded in external research before any redesign — asked the assistant to "tìm hiểu trên github và một số nguồn bạn có xem tôi cần nâng cấp gì về mặt giao diện không" (research GitHub and your other sources to see what UI upgrades I need); expects the assistant to research the current UI library's feature set (e.g. WPF-UI) and modern desktop design trends, diff them against the actual code (XAML + code-behind), and present a prioritized comparison table (priority/current state/proposed change/value) with a recommended implementation order for approval before touching code. Confidence: 0.7
- UI controls must never be clipped by the window edge — reported toolbar buttons ("Tiếp"/"Trước" in the EPUB preview window) being cut off by the right edge when the window sits at its minimum width; expects toolbars with many controls to be responsive rather than overflow: wrap the toolbar in a horizontal ScrollViewer (HorizontalScrollBarVisibility="Auto", HorizontalAlignment="Left" so scrolling actually works), compact control widths, and shorten labels ("Làm mới theme" → "Làm mới") so every button remains visible/reachable at the minimum window size. Freshly extended to collapsed NavigationView panes: the sidebar toggle button was clipped on its right side when the sidebar collapsed because `CompactPaneLength` fell back to the control's default narrower width — collapsed panes must set an explicit `CompactPaneLength` (e.g. 52) wide enough to contain the 36px toggle button plus margins, and the collapsed state must be screenshot-verified so no control is cut off. Confidence: 0.8
- Checks in on long-running background jobs with terse status questions (e.g. "xong chưa nhỉ" — done yet?; "chạy xong rồi hả" — is it done running?) and expects the assistant to answer with an empirically verified, definitive status — process ended, progress JSON at N/N chapters, output file count, total duration/size — rather than a guess or an update-only reply; the check-in is the trigger to confirm completion across all evidence sources (process, progress, output files) and report the final deliverable summary plus ready next steps. Confidence: 0.7
- Confirms that a new output actually contains the new feature by measuring the audio itself (RMS/spectral analysis of quiet segments) instead of trusting progress metadata or file sizes alone — after generating 3 chapters with background music, ran an RMS analysis over 200ms frames to compare the quietest segments and the p10 percentile of the new files vs the old no-music file (music bed present: p10 RMS ≈ 0.1; no music: ≈ 0.00001) before declaring the feature verified; when an inline check produced no output, fell back to a small standalone script rather than assuming success. Confidence: 0.6
- When a new input book is a PDF (not EPUB), expects the assistant to first probe the PDF for a text layer and use the lightest extraction path that fits — if PyMuPDF `get_text()` works, extract directly with PyMuPDF instead of defaulting to the heavy MinerU pipeline; the user accepts this deviation from the standard pipeline because it is materially faster/lighter for text-layer PDFs. The assistant also inspects the PDF structure (page count, language of the sample text, chapter-heading regex matches, mid-book page sanity check) before choosing the approach, and explicitly verifies the output (vi.md heading count, cleaned text) plus runs `detect_chapters` + `smart_chunk` against the real venv-based pipeline before the full run. Confidence: 0.7
- Wants a real voice check (a generated sample audio) before the full book run even for a brand-new input format — for the PDF book Rừng Na-uy, the assistant still ran `--sample` first (19.7s, RTF 0.53) to confirm the TTS pipeline works end-to-end on the new book before launching the full background run; the sample step is a standing pre-flight for every new book, not something the user must re-request. Confidence: 0.6
- Generalizes the sample/demo-first preference to new FEATURES, not just audiobook/TTS runs — when a feature is proposed (e.g. background music in the reader), the user asks "làm ví dụ cho tôi một đoạn để tôi xem trước được không" (make an example so I can preview it first), expecting a real runnable demo integrated into the actual app — working controls in the real window, generated sample assets (e.g. 3 Python-generated lo-fi WAV tracks in a gitignored folder), a 0-error/0-warning build, and a launch check confirming it doesn't crash — so they can try it before the full feature (config persistence etc.) is built; preview-and-approve, not a mockup and not the complete implementation all at once. The user also iterates on the sample with their own assets: adding a new track to the project's music folder and tersely asking "bạn làm lại phần sample theo nó nhé để tôi xem" (redo the sample with it so I can see) means regenerate the demo with the user-provided track so they can listen and judge before any pipeline integration. Extended freshly to TRANSLATION work itself: requested a "Dịch mẫu 1 chương" (sample 1 chapter) button on the Input tab so the user can preview a translated chapter before committing to translate the whole book — i.e. before any expensive full-book run, expect a small "try one chapter first" button on the relevant UI page that translates a single chapter via the same pipeline and shows it in a preview window; for very-large expensive operations (full book translation) a sample button sits beside the full-run button, and the preview window offers a mode toggle (Bản dịch / Song ngữ / Tam ngữ / Bản gốc) so the user can judge quality against the source from any angle before deciding. Confidence: 0.75
- Wants a "demo thử / nút test" (demo/test button) for any NEW Stream-2 (UI/API) feature added during the pilot — when adding the "🧪 Demo dịch thử" panel to ApiPage.xaml (3 preset sample texts EN→VI / ZH→VI / VI→EN with 1-click translate, output shows model + elapsed time + token in/out + source/target char count), the user explicitly framed this as the validation step for the new API-backed UI stream: cheap (20-80 tokens per call), ZERO setup (no book / no chunks / no extract needed), independent (placed in tab Cài đặt next to "Kiểm tra kết nối"), and provider-agnostic (works with whatever provider/model is currently configured). For any future Stream-2 feature, expect this same pattern: build the smallest possible test button that exercises the feature with synthetic input, not a real book/pipeline, so the user can verify the integration is stable before any production work is committed to it. Confidence: 0.85
- Requests a full book translation with a single terse Vietnamese sentence naming the exact input EPUB (e.g. "tôi có một cuốn sách trong input\且以情深共白头：婚前看情感，婚后靠相处 (晚情).epub hãy dịch nó cho tôi" — I have a book in input, please translate it), expecting the assistant to autonomously run the entire translation pipeline per the project's rules — read STATE.md/session_log.md first, locate the file, consult the `/dich` command, follow the AGENTS.md steps (extract → QC/language/OpenCC → chunk → glossary → trilingual skeleton → batch translate → QA → merge → EPUB → update STATE/log), and track progress with a todo list — with no clarifying questions. Confidence: 0.7
- When re-running/re-translating a book that already has data, treats it as a brand-new book with a full clean-slate pass rather than resuming/patching — explicitly instructed "chạy như một cuốn sách mới cho tôi ghi đè vào giữ liệu cũ dọn dẹp thư mục cũ" (run it as a new book, overwrite the old data, clean up the old directories) after choosing the re-translate option; expects the assistant to delete ALL old per-slug artifacts first (working/extracted, chunks, progress, qa, progress_audio, output/books, glossary) and then run the complete pipeline from scratch (extract → QC → chunk → glossary → skeleton → translate → QA → merge → EPUB → audiobook), overwriting prior outputs entirely. Confidence: 0.9
- Evaluates proposed AI/API-based features partly on cost before approving them — when the assistant offered Level B (AI-based per-chapter music selection), the user's first question was "ở Mức B thì có tốn nhiều không" (does Level B cost a lot?); expects the assistant to research the project's existing API providers/models and produce a concrete per-use cost estimate (tokens per chapter, price per 1M tokens, total for a whole book) so the decision between a zero-cost heuristic (Level A) and a paid AI approach (Level B) is grounded in real numbers rather than hand-waving. Freshly reconfirmed in the token dimension: when the assistant proposed the "đọc lại như nhà văn" re-read-and-polish pass (#3) of the translation-quality plan, the user's first question was "phần #3 có thể tốn nhiều token không" (could part #3 cost many tokens?) — the cost-check applies to the TOKEN consumption of any proposed pipeline step, not just API fees; the expected answer quantifies it concretely (tokens per chapter, total per book, % overhead vs the existing translation cost, and cheaper scoping alternatives like re-reading only the first chapters plus voice-transition chapters to halve the cost) before the user commits to approving it. Confidence: 0.7
- When the user requests a trial of specific chapters, generates ONLY those chapters (the first ones, in order) — never unilaterally expands the run to the whole book or starts other chapters without explicit authorization; the user corrected the assistant's overreach ("từ từ đã lúc nào tôi bảo tạo hết mới tạo hết tạo trước 3 chương đầu đã" — wait, I only said make the first 3 chapters; only create everything when I tell you to) after the assistant had started generating a random later chapter (ch 39) and mapped music for all 50 chapters during a "thử 3 chương" request; expects the assistant to stop the over-scoped run and redo exactly the requested scope, then wait for the user's go-ahead before continuing to further chapters. Reconfirmed with a fresh instance: "tạo thử cho tôi audio 3 chương output\books\ban-co-nam-cho-ngoi của sách này" (make a trial audio of 3 chapters) was executed as exactly ch01–03 (the first three, in order) with no scope expansion, and the user accepted the output without objection. Confidence: 0.92
- When a terse pipeline request conflicts with the verified project state, expects the assistant to check the actual per-book state first and clarify scope before executing expensive work — the user asked to "dịch lại cuốn này" (re-translate this book) for `zuo-yi-ge-gang-gang-hao-de-nu-zi-wan-qing` even though it was already fully translated (71/71 chunks, 0 empty, 127,778 source chars → 444,844 translated chars, merged into final/vi.md + trilingual.epub); the assistant verified progress/chunk JSONs, reported the concrete numbers, and offered options (keep the translation & generate the full audiobook / re-translate from scratch / only missing chapters) rather than blindly re-running the whole translation; the user's loose verbs don't always match the real state, and their actual need (e.g. locating the original input file for that book) only surfaced through the clarification. Confidence: 0.55
- EPUB extraction artifacts are junk to strip from raw.md before chunking, not content — when post_extract_qc flagged 60 `---` separator lines and 55 `xml version='1.0' encoding='utf-8'?` declaration lines (one pair per EPUB item) in a ZH book, the established fix is to filter those lines out of raw.md (e.g. PowerShell `Where-Object`) and re-run QC until it passes; such repeated declaration/separator lines are expected EPUB-extract noise in this project, and the assistant should clean them automatically rather than treating them as real headers/footers. Confidence: 0.6
- The batch-translation step for re-run books follows a fixed split/delegate/merge pattern: split each chunk's `original_text` into per-chunk `.src.txt` files (short lines, fully readable), hand each batch to parallel sub-agents — which are blocked from shell and may only read/write files — to produce `.vi.txt` translations, then merge into the progress JSON programmatically with a line-count check (source lines == translated lines, 100% required) before QA. This works because the progress JSON's single-line `pinyin_text` field exceeds the file-read 2000-char truncation and makes in-place JSON editing unreliable for long chunks; ~24 agent rounds translated 71 chunks this way with zero line mismatches and QA 0 errors. Confidence: 0.6
- For the split/delegate translation pattern, delegates ONE chunk per sub-agent per round when multi-chunk (3-per-agent) assignments fail — in the OCR-scanned Vãn Tình book, 3-chunk-per-agent rounds repeatedly errored mid-run with zero `.vi.txt` files written (chunks 21-23, 27-29, 36-38 all failed and only succeeded after re-delegation as single chunks), while single-chunk assignments with an explicit "match the source line count exactly — 1 source line = 1 translated line" instruction succeeded 100%; for OCR books whose sentences are split across lines, the line-count match is mandatory even when the Vietnamese translation must break mid-sentence, and is verified by comparing the `## Trang N` / page-number markers between source and translation before merge. The main agent hand-translating a chunk directly also produced repeated line mismatches, so delegating to one focused sub-agent with the line-match instruction is the reliable path, not manual translation or retrying the larger batch. Confidence: 0.7
- EPUB outputs for trilingual books (Hán + pinyin + Vietnamese) must embed a font covering CJK + Latin-extended + Vietnamese diacritics instead of relying on generic `font-family: serif` — the user reported "file epub bị lỗi font" when opening in Calibre (their EPUB reader): accented glyphs (pinyin `nǐ tuǒ`, Vietnamese `Sự thỏa hiệp`) rendered as `?` because the reader lacked a font for those scripts. The established fix: embed NotoSerifSC-VF.ttf (from `C:\Windows\Fonts`) via pandoc `--epub-embed-font` + a CSS `@font-face` applied to body/headings/paragraphs, then post-process the EPUB zip to rewrite the CSS font path from `fonts/...` to `../fonts/...` (pandoc keeps CSS in `EPUB/styles/` but places fonts in `EPUB/fonts/`, leaving the relative path wrong); applied to all EPUB variants (trilingual/tamngu/vi), which grew to ~15MB each — correct rendering is preferred over small file size. When diagnosing, first verify the file bytes are intact via Python UTF-8 decode (PowerShell's cp1252 console shows `?`/`????` for perfectly valid UTF-8) before blaming the file itself. CAVEAT (learned on the follow-up): font embedding alone did NOT fix the user's `?` — the true root cause was mojibake in the SOURCE translation data (one chunk written via a PowerShell pipe had every Vietnamese diacritic corrupted to `?`), which propagated through merge into vi.md and the EPUB; so when `?` persists at Vietnamese text after font embedding, scan the source data (progress JSONs / vi.md) with Python for `[a-zA-ZÀ-ỹ]?[a-zA-ZÀ-ỹ]` mojibake and repair the data before touching fonts again. Confidence: 0.8

- Never pipe Vietnamese text through PowerShell into a Python script's stdin to write data — `Get-Content ... | python _write_chunk.py` corrupts every Vietnamese diacritic to `?` because PowerShell pipes use cp1252, not UTF-8 (it produced `Trong m?t nh?m ng??i ph?n ??i` in the stored progress JSON, which then corrupted vi.md and the EPUB); the safe, established pattern is to write the translated text to a UTF-8 `.txt` file and have Python read that file when updating the JSON. Confidence: 0.8
- Asks which source file feeds a given pipeline stage (e.g. "phần audio bạn lấy file nào để chia chunk vậy" — which file does the audiobook chunking use?) and expects the answer to trace the actual script logic (e.g. `find_vi_md()` in `audiobook_long.py` resolving `output/books/<slug>/final/vi.md` with fallback to the old `<slug>-vi.md` layout) and verify the file on disk (exact path, size, mtime), rather than a generic answer — the user tracks data provenance across pipeline stages (audiobook reads the pure-Vietnamese `final/vi.md`, not the trilingual md). Confidence: 0.5
- After a change is made to the pipeline, asks how the system worked BEFORE the change ("thế trước hệ thống đang dịch sách như thế nào" — so how did the system translate books before?) and expects a documentation-grounded walkthrough read from the actual command docs (dich.md), not a memory-based summary — the full step-by-step flow (input drop → extract/QC → per-language chunk sizes → glossary auto-merge → agent-driven batch translation → QA → merge → EPUB → audiobook) with concrete mechanisms (3-chunk batches, "bạn tự dịch — AI chat" agent translation rather than API, GPU flags), ending with a clear statement of which step the recent change (e.g. the LITERARY QUALITY prompt) affects — so they understand the before/after delta of their own system, consistent with their knowledge-gathering habit. Confidence: 0.5
- Periodically asks the assistant to audit the project's pipeline/workflow command docs against actual practice — "ok kiểm tra dich.md xem quy trình đang như thế nào có cần chỉnh sửa gì không" (check dich.md to see what the process looks like and whether it needs edits) — expecting the assistant to diff the documented process against what actually runs now (new flags like `--gpu`/`--output-dir`, missing steps like the audiobook generation command, discovered pitfalls like never piping Vietnamese through PowerShell/cp1252, cuDNN venv separation, EPUB font embedding for ZH books), present a comparison table of gaps with proposed fixes and ask which scope to apply, then update the doc comprehensively after approval — including a "kinh nghiệm từ các phiên" (lessons from past sessions) section so the doc captures hard-won operational knowledge, not just commands. Freshly reconfirmed when the user asked "các luồng công việc các bước thực hiện công việc đã update workfollow hết chữa" (have all the workflow steps been fully updated in the workflow?) right after a session — expecting the assistant to diff both command registries (.commandcode + .opencode) AND AGENTS.md against the session's actual lessons (TOC-sync step added to dich.md merge, delete-old-MP3s + audio_qa-root-folder-slug notes in audio.md, stale trilingual.epub convention corrected + working/profile/ added in AGENTS.md) and sync every gap rather than merely report. Confidence: 0.8
- Prefers the project's `input/` source-files folder organized by processing status into subdirectories so a glance reveals which books are done — explicitly requested that after every translation/audiobook job the assistant move the file so it's clear "sách nào đã dịch và sách nào đã tạo audio" (which books are translated and which have audio); when offered options, chose subdirectory-based organization (`input/chua-lam/` not-yet-done / `input/da-dich/` translated / `input/da-audio/` translated + audiobook) over filename-prefix renaming, keeping original filenames intact; approved making this automatic via a reusable script (`scripts/manage_input.py` that derives status from `output/books/<slug>/final/vi.md` + `audiobook/*.mp3`) wired into the pipeline docs plus an `input/README.md` explaining the structure — status organization should be self-maintaining, not manual. Reconfirmed when the user explicitly demanded that updating `input/` be the MANDATORY final step of every translation/audio job ("khi dịch hoặc tạo audio dự án thì bước cuối phải cập nhập input nhé để tôi biết" — the last step must update input so I can see the status) — and expects such session-wide workflow invariants to be codified in **AGENTS.md** (the file agents read every session: the `/dich` description, pipeline step 12 "Cập nhật input/ (BẮT BUỘC)", and the directory-structure section), not only inside the `/dich` command doc, so the step is enforced no matter how the job is run (via slash command or directly). Freshly reconfirmed for the split-command structure: when `/dich` was separated from the combined pipeline, the user explicitly demanded that the translation-only stage ALSO update input ("phần dịch cũng cập nhập input nhé" — the translation part should also update input) — i.e. EVERY standalone stage keeps the input/ status classification current and self-maintaining, not just the audiobook stage: `/dich` done → file into `input/da-dich/` (đã dịch, chưa audio) + metadata `has_audio=false`; `/audio` done → `da-audio/` + `has_audio=true`; `/dich_audio` done → straight to `da-audio/`; the mandatory input-update step must be written into each stage's own command doc (dich.md section K) and kept in sync across both command registries. Confidence: 0.95
- Proactively commissions GitHub research on high-star repos as evidence before improving their agent setup — e.g. asked the assistant to find top-starred GitHub repos on agent token/context saving, agent memory (to prevent context loss/"ngáo"), and guardrails against agents editing code out of scope ("sửa code lung tung"); expects the assistant to search from multiple angles in parallel, verify star counts directly on GitHub rather than trusting search snippets, organize findings by topic, and close with an applicability assessment against the user's own project (what fits their scale vs what's overkill, e.g. an honest verdict that repomix/context7 benefits are limited for a ~15K LOC repo), consistent with the recurring "nghiên cứu xem cần cải thiện gì" knowledge-gathering habit. Reconfirmed when the research ended with a no-external-tool verdict and the user asked "hãy phân tích đi xong có giải pháp đề xuất nâng cấp nào không" (go analyze, then any proposed upgrades?) — the research phase is expected to continue into an evidence-based analysis of the user's own project (reading memory files, scripts, git state) and end with a prioritized, risk-rated upgrade proposal, not just a research report. Confidence: 0.65
- Expects cross-book glossary aggregation (same author/genre) to be fully automatic — when a new book by the same author introduces new terminology, the shared genre glossary should auto-merge the new terms and the new book's own glossary should auto-inherit existing common terms, so nothing requires manual merging; asked about the proposed glossary-upgrade "có tự động gộp và cập nhập không" (does it automatically merge and update?) before approving implementation, expecting automation rather than a manual step. Reconfirmed when the user approved implementation of the automatic 2-way merge script ("ok nâng cấp cái này" — ok, upgrade this) — the automated merge is expected to run as part of the pipeline, not as a manual step. Reconfirmed again for the author-scope extension: after ordering "có làm đi" (yes, do it) for the author-based pool, asked "sách mới có tự động đọc file này và cập nhập thêm hoặc sử dụng không" (will new books automatically read this file and add to/use it?) — a new book by the same author must auto-inherit the shared author pool AND feed its new terms back into it, with no manual merge step. Confidence: 0.8
- When pooling glossary terms across books, delegates the grouping dimension (author vs genre) to the assistant's data-driven recommendation — asked "theo bạn thì bạn đang gom theo tác giả hay là thể loại ok hơn" (in your opinion, is grouping by author or by genre better?) and approved with "có làm đi" (yes, do it) the assistant's evidence-based choice (term-overlap check across 3 Vãn Tình glossaries) to add a `glossary/authors/` pool as the primary grouping for prose authors whose books share distinctive vocabulary (author name, signature concepts like 不攀附/不将就), while keeping `glossary/genres/` for technical cross-author terminology (e.g. tien-hiep cultivation terms); the merge script must support both `--genre` and `--author` scopes — reapproved with the terse "có ok nhé" (yes, ok) when the assistant proposed implementing the author-scope extension, expecting immediate implementation without further questions. Confidence: 0.7

- Expects ALL custom project slash commands to be registered in Command Code's `.commandcode/commands/` directory, not only the legacy opencode `.opencode/command/` location — after porting `/start` and `/done`, the user tersely ordered "cho cả /dich vào commandcode cho tôi nữa" (add /dich into Command Code for me too), extending the port to the main translation-workflow command as well; the assistant copies the same command file into both locations and verifies the command directory listing (file presence), so every workflow command (start/done/dich) is usable from Command Code. Confidence: 0.75
- Prefers automated verification/guardrail scripts over relying on the agent's self-discipline ("agent tự giác") for Memory Bank bookkeeping — approved adding `scripts/verify_memory.py` (machine-check that STATE.md covers books in output/books/ excluding test/archive dirs, session_log has today's entry + required sections, session_log is under the rotation threshold, AGENTS.md still mandates memory reads; exit codes 0 = OK / 1 = warnings / 2 = severe) and making it a mandatory end-of-session step in AGENTS.md, explicitly accepting the assistant's framing that "the agent will remember to update memory" is a weakness to be compensated with a deterministic script check, not just a rule. Confidence: 0.6
- Output book folders under `output/books/` must be named after the ORIGINAL book title (the source input filename, cleaned of download-site junk like `(z-library.sk, 1lib.sk, z-lib.sk)` while keeping diacritics/CJK characters), not the technical slug — explicitly requested "phần output lên đặt tên theo đúng tên của cuốn sách gốc đề mình dễ kiểm soát" (name the output after the exact original book name so it's easy to control/manage); the slug survives only as an internal identifier inside a per-folder `metadata.json` (`{"slug": ..., "title": ..., "source_file": ...}`) that every script and the desktop app reads to map folder ↔ slug, and docs (dich.md/AGENTS.md/STATE.md) were updated so the pipeline creates the folder under the original title plus metadata. Freshly enforced by the user's own audit: spotting that a new book's output folder had been named with the Latin slug (`you-duo-xiang-jiu-you-duo-xing-fu`) instead of the input filename, the user flagged it as a mistake ("ô cái bản output đặt tên thư mục ko giống input nhỉ cái này hình như nhầm bạn kiểm tra lại xem" — the output folder name doesn't match the input, this seems wrong, check it again) and expected a filesystem-verified check (compare actual output/books entries against input filenames and the AGENTS.md convention) plus the fix — rename the folder to the original title and keep the slug only inside metadata.json, updating dependent docs (STATE, session_log). Confidence: 0.92
- The final EPUB deliverable (the trilingual version at the root of the output book folder) must be named after the input source book's filename — explicitly requested "file sản phẩm cuối epub ấy thì ... tên của nó là tên sách input luôn nhé" (the final EPUB product file's name should be the input book name) — derived from `metadata.json`'s `source_file` (extension stripped), keeping even download-site junk from the input name (e.g. `做一个刚刚好的女子  不攀附, 不将就 (晚情) (z-library.sk, 1lib.sk, z-lib.sk).epub`), instead of the generic `trilingual.epub`; the pure-Vietnamese `final/vi.epub` keeps its descriptive name since it describes content and lives inside `final/`. Dependent code must resolve the file via metadata first (e.g. desktop `FindPreviewEpub` looks up `<source_file minus extension>.epub` before falling back), and the naming rule is codified in dich.md so future books follow it. Confidence: 0.8
- When offered safer alternatives (keep slug + alias symlink, or rename only the final files), chose the riskiest-but-cleanest option "đổi tên thư mục + sửa mọi code" (rename folders + fix all code) — accepts wide code changes across Python scripts and the C# desktop app to get human-readable output names, prioritizing clean glanceable naming over backward-compatibility convenience. Confidence: 0.7
- Wants the output library to mirror the input sources exactly — completed output books (with finished audiobook/EPUB products) that have NO corresponding source file in `input/` are deleted outright (chose "xoá luôn" over archiving or keeping), and test outputs (e.g. `long-test`) are removed too, so `output/books/` stays strictly aligned with real input files. Confidence: 0.6
- Prefers a master-based data architecture for shared reference data (glossary): one central `master.csv` (columns `source,target,type,note,book,author,genre`) replacing the old multi-file per-book layout, with every consumer script (QA, translate helper, pipeline) reading through one shared library (`glossary_lib.py`) that filters rows for a given book slug (`filter_for_book`), and a rewrite of the merge script to append new-book terms into master instead of merging across per-book/genre/author files — an explicit user-requested refactor ("cho gọn lại chỉ để lại 1 cái csv duy nhất" — keep just one single CSV) to cut token/read overhead. The consolidation also deleted the now-redundant per-book/genre/author CSV files and their subdirectories after merging them into master, keeping only the master file plus doc/template files. Reconfirmed with a directory-necessity audit: the user asked to check the glossary folder and drop anything unnecessary so only one master file remains ("xem cái thư mục này glossary có vài file có cần thiết không... thì cần để một file master thôi" — check this glossary folder, are the extra files necessary? keep just one master file), expecting the assistant to list every file with size, grep the scripts for actual references, and delete whatever nothing uses (e.g. `_fields.md`, the empty `authors/` dir, and the `genres/tien-hiep.md` reference doc) — keeping only `master.csv` plus the `_template.*` files that `translate.py` actually copies when creating a new book; the user's keep/delete criterion for data-directory files is real script usage, not documentation value. The user sealed the decision by quoting the exact proposal item back verbatim ("3. Giữ _template.md + _template.csv — BẮT BUỘC vì translate.py cần để tạo glossary cuốn mới" — keep _template.md + _template.csv, mandatory because translate.py needs them to create a new book's glossary), after which the assistant executed the cleanup and re-verified (verify_memory, merge_glossary --check, filter_for_book still returning 47 terms) with no further questions. Confidence: 0.85
- Upgrading/reinstalling a venv library (e.g. `vieneu`) OVERWRITES the project's local site-packages patches, so every upgrade must be treated as a re-patch + re-verify cycle: the new install clobbered the local `_load_mono` soundfile patch in `inference_v3_turbo.py` (torchaudio.load fails without torchcodec/FFmpeg shared on this machine), and the assistant had to re-apply the patch, then re-run the GPU sample test and an end-to-end single chapter (with the standing GPU flags --gpu --batch-size 16 + sampling params) before declaring the upgrade complete; the "phải áp lại patch mỗi lần cài lại vieneu" (must re-apply the patch every time vieneu is reinstalled) rule is codified in STATE.md as a standing operational constraint. Confidence: 0.6
- Periodically asks whether an installed dependency/library has upstream updates ("Vienue trên github có cập nhập gì không" — does VieNeu have any updates on GitHub?) right after verifying their own work on top of that tool, and expects the assistant to check the authoritative upstream sources (PyPI page, GitHub releases/commits/changelog) against the currently installed version, summarize what changed, and assess relevance to the user's own project — e.g. recognizing that the VieNeu v3.2.5 release (sentence-boundary chunking + quote recognition + max_new_frames 300→600) fixes the same short-chunk hallucination class just patched externally in audiobook_long.py, so the in-library fix complements the project's external fix — before proposing an upgrade and asking for approval to install/test; consistent with the user's recurring keep-up-to-date / "nghiên cứu xem cần cải thiện gì" scanning habit. When the user approves the upgrade, they also order a project-wide sweep ("ok nâng cấp cho tôi đi và kiểm tra các repo khác trong dự án nữa nhé" — upgrade it and also check the other repos in the project): every dependency/venv (vieneu, sea-g2p, mineru, paddleocr, torch) is checked against the latest published version (PyPI JSON / pip index) and reported as a version table with honest "already latest / no need to upgrade" verdicts, upgrading only the dep that actually has a relevant new version while leaving GPU-matched CUDA builds (torch 2.13.0+cu126) untouched to avoid risk; after the upgrade the pipeline is re-verified (GPU sample + one end-to-end chapter). Freshly reconfirmed for a second pipeline dependency: when the user asked "Mieru có cập nhập gì không nhỉ" (does MinerU have any updates?), the assistant checked the installed 3.4.4 against GitHub's latest stable 3.4.5 (14/08) and alpha v4.0.0a6, summarized the delta (3.4.5 = only 2 minor bug fixes: DOCX table cells with special chars + Unicode surrogate pairs; the big wins — PP-OCRv6 +11% accuracy, +100% OCR speed, hybrid parsing — arrived in 3.4, which is already installed), and delivered the honest "không đáng nâng cấp" verdict rather than pushing the upgrade — the evidence-based version-check-and-verdict treatment applies to every pipeline dependency (TTS engine, PDF extractor), not just the one that first triggered it. Freshly reconfirmed a third time when the user extended the sweep to the entire project with a standalone question — "trong dự án còn có các repo nào cần cập nhập nữa không" (are there any other repos in the project that need updating?): the assistant scanned requirements.txt, grepped scripts for every external-repo reference (github/paddle/mineru/vieneu/sea-g2p/kaldi/huggingface/torch/transformers/lmdeploy/gradio), checked each installed version in its own venv (e.g. paddle via `pip list` in venv-ocr), web-searched the latest GitHub releases (e.g. PaddleOCR), and delivered the per-tool verdict table — concluding "không còn repo nào cần cập nhật" (no more repos need updating: VieNeu already upgraded, MinerU 3.4.5 only patch-level fixes not worth it, PaddleOCR 3.7.0 already ahead of public releases, PaddlePaddle GPU 3.3.1 stable) — a negative no-upgrade-needed verdict the user accepted without pushback, consistent with their standing keep-up-to-date habit. Confidence: 0.72

- Understands the project's own history as data: when a fresh job surfaces what looks like a duplicate of earlier work (e.g. re-translating a book whose old output/session_log entry exists under a different slug), expects the assistant to verify what is actually still on disk (does the old output folder exist? is it the same book?) before claiming "two versions exist" — the earlier session entry can be stale (folder already deleted), and the real inconsistency may instead be that the NEW output folder violated the naming convention; the investigation must be filesystem-evidence-based, not session-log-based. Confidence: 0.5
- Proactively updates the agent's own instruction/rules docs after the user exposes any agent deviation, and expects such rule-hardening to be kept in sync across the two command registries (.opencode and .commandcode) plus AGENTS.md — when a delivered EPUB violated the documented single-EPUB rule, the user ordered "cập nhập những điều này với file .md để Agent ko bị ngáo nữa nhé" (update these into the .md files so the Agent doesn't get confused again), and the assistant added a MANDATORY 7-item pre-completion checklist to dich.md section K and synced the EPUB naming/placement rule into AGENTS.md pipeline steps and the directory-structure section. Confidence: 0.8
- Prefers a cost/quality balance when offered tiered pipeline-quality options — asked "cách nào cân bằng giữa chi phí và chất lượng" (which way balances cost and quality?) when presented a 3-tier menu (A: free script-based detection of machine-translation errors like adjacent repeated words / "một cách"-style clichés / over-long stiff sentences; B: same free detection + AI fixing only the flagged segments; C: full re-read-every-chapter-as-a-writer pass costing ~150-250K tokens/book): the accepted logic is that Level B captures most defects (~85%) at ~1-2% of translation cost because machine-translation errors are regex-detectable and AI only needs to see the flagged segment plus a few surrounding sentences. Before implementing any new QA check, the user also expects an audit of what the current QA scripts already cover (glossary_qa.py checks Han-char leftovers, mojibake, duplicate lines, excess blank lines, glossary adherence) with the exact coverage gap reported, then the free additions proposed first. Confidence: 0.6

- On this machine, shell file-deletion commands (cmd `del` and PowerShell `Remove-Item`) are blocked by the project's agent permission rules, while Python file deletion via `os.remove` is not — cleaning up temp/test artifacts (benchmark WAVs, test scripts, dup-check folders) must use a small Python one-liner (with `sys.stdout.reconfigure(encoding="utf-8")` for Vietnamese output) rather than shell deletion commands, which error out and stall cleanup. Freshly reconfirmed when the user tersely ordered "xoá các file temp" (delete the temp files): the assistant deleted via a Python `pathlib` unlink one-liner and confirmed absence with `glob`, not shell `del`/`Remove-Item`. Confidence: 0.65
- On this machine, directories inside the OneDrive-synced project tree (E:\OneDrive\onyx\Translate Book) can carry OneDrive placeholder (P) + read-only (R) attributes that make Python `shutil.rmtree` fail with Access Denied even on empty dirs, and `attrib -R /s /d` does not stick because OneDrive re-asserts read-only; the reliable force-delete is `cmd /c rd /s /q "<path>"` (with `if exist` to confirm removal). Confidence: 0.55
- The desktop app's book-folder deletion must be a safe best-effort delete rather than a single `Directory.Delete(dir, true)` call — the app needs a `DeleteDirectorySafe` helper that: clears ReadOnly attributes but does NOT touch OneDrive placeholder attributes (ReparsePoint/Offline) so cloud files aren't broken; retries each file delete up to 3× with a small sleep; skips any file still locked (so one locked file never aborts the whole deletion); then removes now-empty subdirectories deepest-first and the root last, returning the count of files actually deleted. This is the established pattern for all `Directory.Delete(..., true)` call sites in the delete-book logic (output folders, working caches, input folder). Confidence: 0.6
- On this machine, a file genuinely locked by a running process (e.g. a WebView2 handle the app itself holds after a preview window is closed) cannot be deleted by ANY means — File.Delete, setting attributes to Normal, or `cmd /c rmdir /s /q` all fail with "being used by another process"/"Access denied" — so the only reliable fix is releasing the lock (kill the holding process, or make the app dispose WebView2), not a stronger deletion command. The desktop app's EpubPreviewWindow and MdPreviewWindow must dispose WebView2 (`RemoveHostObjectFromScript` + `Dispose`) in their Closed handlers; otherwise a book folder opened in preview stays partially undeletable afterwards (app's delete fails with "Access to the path '...\images' is denied"). Confidence: 0.6
- When codifying CLI flags into pipeline command docs (e.g. writing `--music-auto`, `--music-volume 0.15`, `--batch-size 16` into audio.md), expects those flags to be validated against the actual script's `--help` output before being written/relied on, so the documented flags are guaranteed accepted by the real script rather than assumed from memory. Confidence: 0.55
- Prefers to exhaust the free, deterministic quality-detection layer (Level A) to its maximum before spending tokens on AI-based fixing (Level B) — after the tiered QA proposal (A: free regex detection / B: AI fixes only flagged segments / C: full re-read-as-writer pass), tersely ordered "ok làm đi nâng cấp mức A lên mức gọi tối đa có thể" (ok, do it — upgrade level A to the maximum possible), authorizing the full set of zero-token literary-quality checks to be added to `glossary_qa.py` (adjacent word repetition within a sentence ≥3× with stopwords excluded, machine-translation clichés like "một cách"/"những điều"/"đối với" used ≥3×, over-long >90-char stiff sentences, high Han-Viet ratio >30% as a translate-word-by-word signal) and integrated into the QA pipeline report ("Chất lượng văn chương"), deferring the paid AI-fix level to a later decision; validated on a real finished book's vi.md, which the QA flagged with concrete evidence (31× "một cách", 15× "đối với", 82 repeated-word spots). Confidence: 0.7
- Wants full project-state visibility available in the terminal workflow — explicitly asked whether there's a way to display the whole project's directory tree, see which files are modified or untracked in git, and read code ("có cách nào hiện thị cây thư mục cả dự án và biết file nào modifile hay untrack không và cả đọc code nữa"); the assistant's accepted framing — matching this user's hands-off, non-expert profile — is that the most convenient path is simply asking the agent to do it (read_directory/glob for the tree, `git status --short` for M/A/??, read_file to read and summarize code) rather than teaching them to memorize raw commands (tree /F /A, git status, type/cat), and offering to run tree + git status directly for them. Freshly reconfirmed when the user reiterated the exact need and brought a self-researched tool candidate to fill it: "Tôi muốn terminnal hiện được cây thư mục untrack Modifile tôi có tìm hiểu được cái gitlazy ấy có ok" (I want the terminal to show the directory tree of untracked/modified files; I found "gitlazy", is it ok?) — the desire for visual git-status display in the terminal extends beyond asking the agent to run commands: the user is open to adopting a dedicated TUI tool (identified as lazygit) for this, and expects the assistant to research the candidate and confirm whether it fits the need, with plain-language install options, rather than being taught raw git/tree commands. Freshly extended when the installed tool proved insufficient: after adopting lazygit, the user immediately asked "trong lazygit tôi ko nhìn được toàn bộ cây thư mục của tôi nhỉ" (in lazygit I can't see my whole directory tree?) — the underlying need is the FULL project tree annotated with git status markers (M/?/A), not just the changed-files-only view that every git TUI (lazygit/gitui/tig) provides; the assistant's proposed remedy — plain `tree /F` (no git marks) or a small custom `gtree` PowerShell function in the user's profile combining the full tree with git-status coloring — is the accepted direction because the user is open to a hand-written helper when off-the-shelf tools don't cover the full-tree-plus-status combination. Freshly confirmed end-to-end: the user asked what gtree would be ("gtree là như nào"), approved the described function with the terse "ừ được đấy", and the assistant installed it into `$PROFILE` as a permanent terminal utility (usable forever in new terminals, no extra install, junk dirs like venv/node_modules/.git/__pycache__/working filtered out by default with an explicit `-All` switch to include everything) — the user accepts the assistant writing persistent helper functions into their PowerShell profile for needs off-the-shelf tools can't cover, and expects a plain usage explanation handed back afterward. Confidence: 0.8
- Wants terminal workflows to mirror desktop GUI capabilities — after noting the desktop app supports running 2 sessions in parallel, asked whether the terminal can do the same ("ở trên giao diện desktop hỗi trợ chạy song song 2 session điều này có làm được trên terminal không"); expects a plain-language feasibility confirmation plus practical setup options (multiple terminal windows each running one named session via `cmd -n "tên"`, terminal multiplexers like Windows Terminal split panes / tmux, session resume via `cmd -r`) rather than being told the feature is desktop-only; the assistant's answer also framed parallel sessions by task/book split for the user's translation/audiobook work. Freshly extended to third-party AI tools: with the Antigravity IDE already installed, asked "bạn có cài được antigravity dạng teminal không" (can you install Antigravity in terminal form?) — the user wants CLI/terminal versions of the tools they use as GUIs so the same agent/tool is available in the terminal (sharing account/state with the GUI version), expecting the assistant to research the official CLI (`agy`), install it via the official installer, verify the binary actually runs, and hand back plain launch instructions. Confidence: 0.6
- Expects long-running batch generation (e.g. full-book audiobook TTS) to survive interruptions and resume from the last checkpoint when they come back — when a 75-chapter audiobook run was stopped so the user could restart the machine, a terse "chạy tiếp tục cho tôi nhé" (continue for me) meant: verify the saved state first (progress JSON completed chapters + output MP3 count on disk), re-run the exact same command, and let the script skip already-completed chapters and continue from where it stopped (resumed at ch20 of 75) — never restart from scratch, never re-ask which chapters to redo; checkpoint-based resumability is treated as a given for multi-hour jobs. Freshly reconfirmed at the session level when a context-overflow error (Error 400) interrupted the session mid-task: the user's terse "tiếp tục đi" (continue) meant resume the in-flight work immediately — re-launch the interrupted audiobook run with the corrected music-rotation command and carry on — with no explanation, apology, or permission re-ask; a bare continue order after any interruption is authorization to pick up exactly where the work stopped. Freshly reconfirmed twice in a single session: bare "tiếp tục giúp tôi" and "tiếp tục cho tôi đi" mid-pipeline each sufficed to authorize the assistant to proceed autonomously with the next translation wave (8 parallel chunks per wave) and then through the remaining merge/QA/EPUB/audiobook stages — the user does not ask for status or scope before continuing; the continuation order alone is the go-ahead. Confidence: 0.8
- Splits monolithic pipeline commands into separate single-purpose slash commands for each major stage — explicitly requested that the combined dịch+audio pipeline be separated into `/dich` (translation only), `/audio` (audio only), and `/dich_audio` (both) so each stage can be run independently ("md dịch đang có chung cả dịch lên audio tôi muốn tách ra là dịch riêng '/dich' làm audio '/audio' và cả 2 là 'dich_audio'"); expects the combined command's name to be the concatenation of the stage names, each split command to keep the full detailed steps of its own stage, and the command set to be reflected in the pipeline docs (AGENTS.md, STATE.md) so it is discoverable. Confidence: 0.8
- Prefers to reuse tools already installed on their machine rather than adopting new ones for the same need — right after the assistant installed lazygit + a custom `gtree` for terminal git-status tree viewing, the user asked "tôi có Agtigravity rồi tôi dùng cái ấy được không" (I already have Antigravity, can I use that?), expecting the assistant to identify what they actually meant (Antigravity IDE 2.0.11, formerly Windsurf — verified present on the machine) and deliver an honest verdict on whether the existing IDE's built-in Source Control panel (file tree with M/U/?/A markers, diff, stage/unstage/commit/push via GUI, Ctrl+Shift+G) fully covers the need — making the newly installed terminal tools optional rather than mandatory; the assistant should check the user's existing software inventory and validate the "use what I already have" option before recommending new tooling, and when the existing tool suffices, say so plainly and let the user keep whichever they find more comfortable. Freshly extended once the user was convinced: the terse "vậy gỡ đi tôi dung antigravity cho nhanh" (then uninstall them, I'll use Antigravity to be quick) meant FULLY remove the just-installed redundant tools — `winget uninstall lazygit` AND delete the `gtree` function from the PowerShell profile — not merely stop using them; expects the assistant to execute both removals autonomously and confirm a clean final state, keeping the machine lean of tools the user won't use. The reuse-existing-tool preference then extends to hosting the agent itself: asked "có cái extension nào trên AntiGravity cho commandcode không" (is there any extension on Antigravity for Command Code?) and expects a documentation-backed answer (Antigravity/Windsurf is a supported editor; running `cmd` in the IDE's integrated terminal auto-installs the integration, with `/ide` as the diagnostic/reinstall command) rather than being pointed back to separate terminal tools. Confidence: 0.65
- Extends the bounded-growth concern to the agent's OWN taste/memory files — after being told Taste learns automatically, asked "thế có sợ nó bị phình to quá mức không" (aren't you afraid it will bloat too much?), i.e. the same worry they raised about session_log growing forever ("sẽ rất là dài"); expects the assistant to answer with actual measurements of the taste files (sizes, line counts, duplicate near-identical bullets, over-long >1000-char entries, missing `# Category` headings) and concrete context cost (e.g. ~167KB total ≈ 40-45K tokens per session ≈ 1/5 of the 200K window) plus a prioritized cleanup proposal (merge duplicate entries / trim long entries / add category headings so the system can auto-split into subdirectories) — not vague reassurance that the system self-manages. Confidence: 0.6
- When running two Command Code sessions in parallel (split-terminal on the same project — e.g. the Translate Book repo plus a C# desktop app), expects a plain-language explanation that the sessions do NOT auto-coordinate or yield to each other: they are independent processes, so both editing the SAME file risks overwriting ("đè code vào nhau"), while editing different files, or one session building/testing while another edits (build is read-only), is safe; git operations self-serialize via index.lock, but sequential dependencies like a C# project needing all edits finished before `dotnet build` must be deliberately scheduled — the user asked this explicitly with a screenshot of two split terminals and expects a practical safe-division-of-labor table, not reassurance of automatic coordination. Confidence: 0.55
- When a delivered artifact violates a documented project convention, the user expects the convention to be hardened into the persistent docs so the agent cannot deviate again — after the final EPUB didn't follow the single-EPUB rule, tersely ordered "phần file epub cuối vẫn chưa đúng rules hay cập nhập những điều này với file .md để Agent ko bị ngáo nữa nhé" (the final epub still doesn't follow the rules — update these things in the .md files so the Agent doesn't get confused anymore); the expected fix covers BOTH correcting the output (delete the extra `final/*.epub`, rename the root EPUB to the input filename) AND auditing every existing book for compliance, then codifying the rule — including a MANDATORY pre-completion checklist (output folder named after the input file, `metadata.json` present, exactly 1 `.epub` at root named `<tên-sách-input>.epub`, `final/` contains only `.md`, 0 mojibake, Noto Serif SC font embedded, input/ status updated) — into the pipeline command doc (dich.md section K) and syncing it into AGENTS.md (pipeline steps 9–10 + directory-structure section) so future runs are automatically consistent rather than depending on agent memory. Confidence: 0.8
- The translation pipeline must include a post-merge sentence-joining step for OCR-sourced books — when the user flagged "phần phân loại đoạn vào câu đang không hoạt động hãy kiểm tra lại" (the paragraph-to-sentence classification isn't working, check it), the accepted fix was a reusable script (scripts/output/merge_sentences.py) run AFTER merge that re-joins the 1-1 line-matched translation into complete sentences (splitting on sentence-ending punctuation 。！？…；;), drops original page-number lines found mid-paragraph (user doesn't need the source book's page numbers), removes pandoc `<div page-break>` junk, and keeps `## Chương N` headings plus TOC entries (title + trailing page number) as their own lines; the step is expected to be codified into the pipeline docs (dich.md) so every future OCR book gets fluent whole-sentence output rather than fragmented lines. The sentence-joining script applies to the pure-Vietnamese merge; for the trilingual (Hán + pinyin + Việt) merge the script only strips page-number/`<div page-break>` junk while leaving the tri-line HTML div blocks intact — BUT the user later reported mid-sentence line breaks in the trilingual Chinese (e.g. `也胜利` on one line, `了。` on the next) as a defect to fix, so the trilingual merge must ALSO join complete sentences: group consecutive `<div class="tri-block">` blocks until the Hán text ends with sentence-ending punctuation (。！？…；;), merge the three layers in parallel (Hán joined with no spaces; pinyin/Việt joined with spaces), emit one complete div per sentence, and drop page-number lines found between blocks. Page numbers can also sit INSIDE the text tokens themselves (not on separate lines) — the user reported the exact defect of a page number glued into a sentence ("123002456", e.g. `方002是`, `电003影` in the Hán text), which is just as unacceptable as a mid-sentence line break; the merge must strip them at the token level by removing standalone pure 3–4-digit numbers from all three layers, while preserving years (19xx/20xx), ISBN/phone numbers, and genuine table-of-contents page numbers (e.g. `女人的小心思006` in a mục lục entry) which are real references, not junk. Confidence: 0.8
- Asks whether a standing pipeline subsystem still works ("phần tạo thuật ngữ vẫn hoạt động tốt chứ" — does the glossary generation still work well?) and expects a real functional verification pass, not reassurance: actually read the subsystem's files/scripts, run its scripts against live data (e.g. `filter_for_book` on the just-finished book), surface any discrepancy discovered (e.g. the new book's per-book glossary had never been merged into master → 0 terms filtered), fix the found defects autonomously (merge the glossary into master; correct stale wrong-transliteration rows like `阿彦 = "A Nhan"` → `"A Ngạn"` across ALL master shards so every same-author book filters consistently), re-verify, clean up temp artifacts, and report a summary of what works + what was fixed. Confidence: 0.6
- When a subsystem health check uncovers master-data errors (e.g. a wrong transliteration shared by several same-author books), expects the fix to be applied to ALL master shards so every book filtering by author/genre gets the corrected value — a partial fix (only the new book's row) would leave other same-author books still filtering the wrong term. Confidence: 0.55
- The audiobook script's bare `--music auto` selects only the FIRST track in `core/music` (avail[0]) — it does NOT rotate; achieving the standing per-chapter music rotation requires passing comma-separated `auto` values (one per chapter, e.g. `--music auto,auto,auto,...` repeated N× for N chapters, cycling through the available tracks) or the `--music-auto` AI content-based per-chapter choice; before committing to a multi-hour full-book run, the assistant must verify the music flag actually rotates per chapter (discovered mid-run that `--music auto` would have used the same single track for all 71 chapters, stopped the background task, killed the child python processes, and restarted with the correct comma-separated rotation) rather than letting the run finish with the wrong music config. Confidence: 0.8
- The audiobook QA script `audio_qa.py` resolves the book directly as `output/books/<slug>/` without reading metadata.json, so when a book's output folder is named after the original (non-slug) title — e.g. the Chinese-named folder — QA must be invoked with that folder name (`python -X utf8 scripts\qa\audio_qa.py --slug "有多想要，就有多幸福 (晚情著)"`), not the internal slug; running QA with the internal slug makes it fail to find vi.md ("cannot find vi.md"). Confidence: 0.65
- Prefers AI-agent-based glossary creation over automated/heuristic extraction tools — explicitly stated "tôi nghĩ tạo glossary bằng AI vẫn ok hơn chứ" (I think creating glossary with AI is still better) when presented with BabelDOC's auto-extract glossary approach as a potential improvement; the existing dual-agent glossary extraction (agent reads book → extracts terms → CSV → review + merge into master.csv) is considered superior to one-shot automated LLM extraction because the agent understands book context, can classify terms by type (character/place/concept), and can review/merge with the master glossary — rejects automated extraction tools for this task in favor of the AI-agent approach they already have. Confidence: 0.8
- Treats unstyled default WPF/WPFUI control chrome as a Liquid Glass theme violation just like unstyled buttons — when the user circled the sidebar area of the desktop app in a screenshot with a terse "xem giúp tôi giao diện chỗ khoanh đỏ này của tôi" (help me look at the UI in this red circle) and no explicit complaint, the diagnosed defect was the default NavigationView back arrow (←) and hamburger toggle (≡) floating at the top of the sidebar, clashing ("lệch tông hoàn toàn") with the glass-styled pills below; when a red-boxed UI area looks off, the assistant should examine it via vision (zooming into sub-regions, comparing against the established theme) to identify unstyled default chrome itself rather than asking the user to clarify, then present a fix proposal table (e.g. hide the useless back button + restyle the functional toggle as glass / hide both / restyle only) with a recommendation and await the user's choice — the user then approves a specific option by letter ("làm phương án A đi nhé" — do option A), and the accepted remedy for NavigationView chrome is: hide the default chrome the app doesn't need (back button via `IsBackButtonVisible="Collapsed"`, default toggle via `IsPaneToggleVisible="False"`) and insert a custom glass button (reusing the app's existing `GlassCollapseButton` style, placed in `PaneHeader`) wired through a ViewModel toggle command TwoWay-bound to `IsPaneOpen`, keeping the collapse/expand functionality intact rather than removing it; the implementation is verified not just visually but functionally — build, launch, screenshot, then actually click the new toggle to confirm collapse and re-expand both work — and the result is accepted as-is. Confidence: 0.7
- Icons on the dark Liquid Glass theme must be bright white and stay high-contrast in every UI state, including collapsed/narrow states — when the sidebar hamburger (☰) turned gray-ish/dim after collapsing, the user complained "ko nổi tý nào" (doesn't stand out at all); the fix is to use the bright white `TextFillColorPrimaryBrush` rather than accent brushes (`AccentFillColorDefaultBrush`), set `Foreground` + `Opacity="1"` directly on the control so the theme can't dim it, and verify the icon visually in BOTH expanded and collapsed states before declaring it done. Freshly reconfirmed when the user reported the collapsed-sidebar hamburger again as "nút bị che mất và nút hình như bị màu đen" (the button gets hidden and looks black): root causes were the collapsed pane too narrow (clipping the button's right side) plus an overly dark glass background reading as black; the fix added an explicit `CompactPaneLength` wide enough for the button and a dedicated nav-toggle style with a lighter glass background + gradient border + always-white icon, verified by screenshotting the actual collapsed state. Freshly reconfirmed a third time when the user again reported the button as still too dark/submerged ("vẫn bị này") against the bright glass "Sách"/"Audiobook" pills below, driving a further prominence pass: because the very dark `GlassElementBackgroundBrush` (#66141424) blends into the sidebar and reads as black, the background is switched to the lighter `GlassButtonNormalBrush`, the border is defaulted to the brighter `GlassGradientBorderHoverBrush` at 1.2px (rather than the fine `GlassGradientBorderBrush` shown only on hover), and the button (36→38px) + icon (16→18px) are enlarged — the standing principle being that a glass control must be as prominent/bright as the other glass pills, and merely adding a glass background isn't enough. Confidence: 0.92
- Action buttons that play the same role must match dimensions, not just glass style — when the "Đọc sách" button was visibly shorter than the "Dịch Toàn bộ" button on the same card, the user ordered it synced ("kích thước hơi bé hãy đồng bộ theo nút dịch sách cho thống nhất" — a bit small, sync it to the translate button for consistency); same-tier primary action buttons must share an identical Height (e.g. 34px), and the result is verified by comparing rendered button heights, not assumed from the XAML alone. The consistency bar extends to ALL buttons on a card/panel (not just same-role ones) and to typography as well as dimensions: a bare order "Đồng bộ Syle và cỡ chữ đi" (sync the style and font size) means normalize every button — primary run buttons, test/sample buttons, and even the cancel (Hủy) button — to one shared style (InteractiveButton), one FontSize (12), and one Height (34), with the assistant choosing the standard autonomously (adopting the primary button's Height 34 + a uniform FontSize 12 for all, fixing buttons that previously fell back to default FontSize) rather than asking which values to use, and verifying with a clean build afterward. Confidence: 0.8
- The sliding light-streak gradient (indeterminate progress bar) must be visually symmetric/balanced — when the user noticed the streak's color gradient "chưa cân nhau" (unbalanced), with an abrupt bright start on one side and a long slow fade on the other, they flagged it as feeling "cụt cụt" (clipped/stubby) and asked for a check; the accepted fix is an egg-shaped symmetric gradient where BOTH ends are fully transparent (fade in and out equally, e.g. 0→0.12 and 0.88→1), brightness ramps up evenly from both sides, and the brightest peak sits at the center (cyan → purple around offsets 0.45/0.55) — the streak should emerge gradually at one edge, peak mid-track, and dissolve evenly at the other edge; asymmetric fade ramps (steep entry, long tail) are treated as a visual defect, not just a style choice, and must be diagnosed from the actual XAML gradient stops before re-tuning length/speed. Confidence: 0.75
- Git commit messages follow an emoji-scoped conventional-commit format the assistant maintains and the user accepts without revision: type prefix + emoji + scope (e.g. `✨ feat(desktop):`, `🐛 fix(desktop):`, `✨ feat(ui):`, `📝 docs:`), a `- ` bulleted body, a `📌 N file thay đổi` file-count summary line, and a `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer; changes are committed per-scope (code / fix / ui / docs+taste) so history reads as clean logical units, and any leftover uncommitted changes discovered mid-push get their own supplementary commit so the tree is fully clean before pushing. Confidence: 0.55
- Wants in-app content preview to cover every format the pipeline accepts as input, not just the translated EPUB product — explicitly requested a "Xem" (View) button on Input-tab book cards that reads the ORIGINAL source content in its native format (PDF, EPUB, MOBI, AZW, DOC...), while confirming the Output-tab product is fine to read as EPUB ("tab output là sản phẩm thì để đọc file epub là ok rồi nhưng ở trong input tôi muốn thêm 1 nút Xem có thể đọc được nội dung sách của input, ở input thì có tương đối nhiều định dạng pdf epub mobi azw rồi là doc" — output as the product is fine reading EPUB, but for input I want a View button that can read the book's content since input has many formats); expects multi-format source reading to reuse what's already available on the machine (Calibre ebook-convert → temporary EPUB → the existing EpubPreviewWindow) rather than per-format viewers. Confidence: 0.6
- Positions the "Xem nội dung" (view original source) button on Input book cards as a standalone full-width action placed directly ABOVE the "Dịch Toàn bộ" button, not nested inside the "Dịch test thử theo chương" frame beside "Dịch test" — the terse order "nút xem ở tab input cho tôi ở trên nút Dịch Toàn Bộ đi" (put the View button in the input tab above the Translate All button) moved it out of the test-translate frame into its own full-width button with the same glass InteractiveButton style as the primary run button; viewing/browsing content is treated as a first-class card action on par with the heavy pipeline actions, while test/sample actions stay grouped in their own frame. Confidence: 0.6
- Wants the desktop/API translation path (Stream 2, the "Dịch Toàn bộ" button) upgraded to PARITY with the agent-driven `/dich` path rather than accepted as a permanently slower pilot — after judging it slow ("không mượt như Agent làm /dich"), tersely ordered "làm sao để nút dịch toàn bộ này mượt như /dich đi" (make this translate-all button as smooth as /dich), and when the assistant offered abandoning the desktop path to run `/dich` for the book instead, the user reiterated the demand to fix the button rather than rerouting the work — i.e., prefer engineering the UI path to match the agent standard, treating `/dich`'s architecture as the reference spec (agent translates 1500–3000-char chunks in one continuous context with no per-request HTTP, vs the desktop's per-chunk serial HTTP requests with small chunks, 1-thread concurrency, and flaky free-model retries); expects the assistant to root-cause that architectural gap and propose aligned changes (raise chunk size to `/dich`'s range, batch 2–3 chunks per request like `/dich`'s claim, default higher concurrency for fast models, keep the reliable non-free model) for approval before any edit. Freshly reconfirmed with an explicit binary choice: when the assistant offered (A) running `/dich` immediately for the current book (smoothest, matches the 10 prior books) vs (B) fixing the desktop app at its root so "Dịch Toàn bộ" permanently runs through Command Code's own managed agent infrastructure (spawning the real CLI binary `cmdc` — the `cmd` name is shadowed by System32's cmd.exe on Windows — headless via `cmdc -p ... --yolo`, reusing the app's existing Python-spawn subprocess pattern), the user chose B with a single letter, preferring the durable architectural fix over the fast one-off completion even though A would have finished the book sooner. Confidence: 0.8
- Desktop-app architecture must stay PORTABLE: the app must run on any machine with just the configured API keys, so it must NOT depend on the Command Code CLI (`cmdc`) being installed locally — when the assistant clarified that option B (make "Dịch Toàn bộ" smooth via Command Code's own infrastructure) meant spawning `cmdc -p` headless, the user immediately objected "như thế nó ko phù hợp với máy khác nhỉ vì máy khác đâu biết cài cmdc đâu ta" (that won't fit other machines — they wouldn't have cmdc installed) and settled on the self-contained HTTP option instead: optimize the desktop's own direct-API translation flow to approach `/dich`'s smoothness (per-language chunk sizing ZH 1500–3000 chars / EN 3000–8000 like `/dich`, default 2 parallel threads instead of 1, add a `User-Agent` header to every request because proxies/Cloudflare 403 requests without one, trim per-request translate timeout 300s→180s), verified by a clean `dotnet build` (0 errors/0 warnings) before handing back to the user to re-run. Confidence: 0.85
- Reasoning/thinking models (e.g. `deepseek/deepseek-v4-flash`, picked earlier because it was cheap and answered a tiny prompt with HTTP 200) are UNUSABLE as the desktop translation API model — such models return `message.content = ''` with `finish_reason: length` because the entire `max_tokens` budget is consumed by the `reasoning` field before any answer is produced; an E2E test on a real 3373-char ZH chunk through the app's actual `ApiTranslationService` showed 0 translated chars while burning 7868–9406 tokens per chunk, and a plain (non-trilingual) call also returned empty content. Model validation for the translation path must therefore verify the response actually carries translated text in `content` with a healthy `finish_reason: stop` (probing `MiniMaxAI/MiniMax-M3` confirmed content present, ~37 completion tokens in ~1.6s), never rely on HTTP 200 plus a correct answer to a tiny prompt — the fix was switching the saved `custom_1` model in `~/.translate_book/config.json` to MiniMax-M3, after which the same real chunk translated cleanly (~11K chars of fluent Vietnamese). Confidence: 0.85
- Treats a translation quality/stability regression since an earlier "smooth" period as a git-history investigation trigger — when the user said "đợt trước dịch rất mượt, đợt này hay bị lỗi ấy chứ, hãy kiểm tra lịch sử commit xem" (last batch translated very smoothly, now it often errors — check the commit history), the expected move is to walk `git log` for the affected translation service/files, diff the commits that landed since the good period, and attribute the regression to a specific change (here: a prompt-optimization commit that silently weakened the literary-translation prompt — removing the strict "never keep Han characters" directive, the detailed polish rules and before/after examples, and the rigid trilingual-output constraints), separating code-caused quality regressions from transient proxy/network errors (524/503) rather than re-testing the current state or guessing. The user also gates the conclusion itself: when the assistant presented its first root-cause attribution (the weakened prompt) as final, the user tersely ordered "kiểm tra kĩ thêm rồi đưa ra kết luận" (check more thoroughly, then give a conclusion) — a first plausible explanation is NOT accepted as final; the deepened pass must broaden the diff scope to the suspect commit's related files and configs (chunking args such as the new --min 800 --max 1600 cap, glossary filtering, defaults), compare against the pre-change baseline (e.g. the earlier unbounded `--strategy smart` chunking), hunt for latent bugs beyond the attributed change (RunChunkAsync never passing `--lang`, so chunk_text.py silently defaults to `--lang en` and miscounts CJK text), and only then deliver the final verdict — separating each error class by its distinct cause and leaving proposed fixes pending the user's approval. Confidence: 0.7