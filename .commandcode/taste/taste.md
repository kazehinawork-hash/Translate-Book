- Communicates in Vietnamese — user writes messages in Vietnamese, and expects responses in Vietnamese. Confidence: 0.9
- Judges the quality of translated literary books by how naturally they read in Vietnamese — explicitly stated "phần dịch cần cải thiện dị cho văn chương lại láng không" (the translation part needs improving so literature reads smoothly), meaning translated prose must be flowing, literary Vietnamese (whole-sentence/paragraph translation, natural dialogue, consistent pronoun systems, idiomatic equivalents for source idioms, avoidance of machine-translation clichés like "một cách"/"những điều"/"mà còn") rather than stiff literal translation that clings to the source sentence structure; when asked what to improve, considers the TTS/VieNeu side already good ("VieNeu là ngon lắm rồi") and directs improvement effort at translation output quality — and expects that guidance to be codified into the translation prompt itself so every future book benefits, not a one-off fix. Freshly reconfirmed with an escalated bar: after the first prompt-level improvement, the user pushed further with "làm để AI nó dịch mượt mà và văn chương lại láng như nhà văn" (make the AI translate smoothly and literarily, as polished as a writer) — the quality target is writer/professional-translator-level polish, treated as a standing goal that warrants a deeper systematic approach (root-cause analysis, per-book voice profiles, reference examples, revision passes) rather than a single prompt tweak. Confidence: 0.75
- Balances "láng" (fluent, literary Vietnamese) with fidelity to the original's soul/essence — explicitly ordered "dịch lại láng nhưng vẫn phải giữ được cái hồn cái chất của văn bản gốc nhé" (translate fluidly but still preserve the soul and character of the original text), meaning "láng" is only the manner of expression while the core must stay faithful: absolutely NO adding meaning, dropping meaning, changing logic, softening nuance, or exaggerating the original for the sake of beauty; the translation must keep the original's tone (sarcastic/lyrical/harsh), narrator's viewpoint, author's attitude toward characters/events, concrete details, numbers, and narrative order — a heavy original sentence must translate heavy, a soft one soft, never "beautified" into a different meaning. Confidence: 0.85
- Prefers consolidating many small data files (e.g. per-book glossary CSVs) into a single master file so the agent reads just one file instead of several — explicitly motivated by token savings: "cho gọn lại chỉ để lại 1 cái csv duy nhất ... để đọc tiết kiệm token hơn" (make it compact, keep only one CSV, to save tokens when reading); once the assistant points out that reading N files costs tokens, the user wants the layout changed to minimize agent read overhead rather than accepting the multi-file structure. Reconfirmed when the user approved "cách A" (way A) of consolidating into one master CSV and praised the per-book/author/genre column structure ("có cột chia từng cuốn từng tác giả khá hay đó" — columns separating each book/author are quite nice) for preserving each row's provenance; expects such a master file to AUTO-SPLIT into numbered shards (master.csv, master_001.csv, ...) whenever it bloats with many books/terms ("khi nhiều sách nhiều thuật ngữ hãy tự tách thành nhiều file master nhé" — when there are many books and terms, auto-split into multiple master files) rather than ever growing into one giant file, with readers transparently re-merging the shards. Reconfirmed with a fresh design-rationale question: the user probed whether splitting into per-book files would make the AI re-derive that book's terms on every run ("nếu tách ra một cuốn 1 thuật ngữ thì kiểu AI sẽ mỗi lần chạy lại phải chạy ra thuật ngữ của cuốn đấy lặp đi lặp lại nhiều đúng không" — if split per book, the AI would have to re-run/re-derive that book's terms repeatedly, right?), i.e. the master design is valued not only for one-shot reading but also for preventing repeated term re-derivation/guessing (and resulting drift) across runs — common terms are written once and auto-filtered per book/author/genre so the agent never re-derives them. Confidence: 0.9
- Is a hands-off, non-expert user on tooling/infrastructure concepts — when told a tool works over MCP, objected "nhưng tôi ko có MCP" (but I don't have MCP), revealing they don't track which infrastructure they have and don't want to set anything up themselves; expects the assistant to explain such concepts in plain, non-technical terms (e.g. "MCP is just a protocol the agent runs automatically, you don't install or configure it") and to reassure them the tool auto-registers/self-configures (e.g. `install` wiring the MCP server for Command Code with no manual steps), offering a hands-on install that doesn't touch their existing config (`--skip-config`). Reconfirmed with a fresh diagnostic question about CUDA: seeing `cuda:0` in tool output, the user asked "sao cuda lại bầng 0 nhỉ" (why is cuda 0?), mistaking the GPU device index for a 0% utilization value — expecting the assistant to first check the live state (nvidia-smi utilization/memory + torch.cuda.is_available) and then explain look-alike technical terms plainly (device index `cuda:0` vs `utilization.gpu = 0%`, and that 0% just means the GPU is idle and healthy, not broken). Confidence: 0.6
- Prefers maximum performance via full GPU acceleration across ALL heavy pipeline stages, not just some — after the TTS GPU setup, explicitly directed "ok dùng full GPU cho mạnh nhé" (ok, use full GPU for maximum power), meaning MinerU (PDF extraction), PaddleOCR (scanned-PDF OCR), and TTS (audiobook) should all run on the RTX 3060; approved the resulting large environment changes (torch/paddle CUDA upgrades totaling several GB, and a dedicated `venv-ocr` without torch to sidestep the torch-vs-paddle cuDNN DLL conflict on Windows) rather than leaving any stage on CPU, and expects the assistant to verify each framework actually detects the GPU after the change. Confidence: 0.7
- When evaluating a proposed tool/feature against their machine, expects an environment-scope check — a discovery like "python global has paddle CUDA but torch CPU, .venv has torch CPU + paddle CPU, venv-vieneu has torch CUDA" must be verified per-environment (each interpreter checked separately) before concluding which env actually runs a given pipeline stage; never assumes the GPU capability of one env extends to another, and treats "which interpreter runs the CLI (e.g. MinerU lives in .venv)" as the decisive fact. Confidence: 0.55
- Prefers to keep the CUDA build version matched to the user's machine instead of just any CUDA-compatible wheel — when torch CUDA was installed as cu126 (matching RTX 3060) and paddle 3.3.1 was pulled from the PaddlePaddle cu126 index to match, the assistant flagged the residual CUDNN 9.9 (paddle) vs 9.5 (torch) warning as acceptable and deferred fixing it — no new learning. Confidence: 0.5
- Fixes the extract-pipeline tool priority: MinerU is the PRIMARY extraction tool (always preferred), PaddleOCR is ONLY a backup/fallback for when MinerU handles input poorly (e.g. scanned PDFs) — the user stated this explicitly and instructed "không thay đổi thứ tự ưu tiên này" (don't change this priority order), so any extract-pipeline work must keep MinerU first and never promote PaddleOCR; both still run on GPU in their separate venvs (MinerU → `.venv`/torch CUDA, PaddleOCR → `working/venv-ocr`/paddle GPU without torch). Confidence: 0.85
- Treats the Windows torch-vs-paddle cuDNN DLL conflict as a real, actionable constraint — learned during the full-GPU work: importing torch CUDA and paddle GPU in the SAME Python process fails (WinError 127, `cudnn_cnn64_9.dll` / `shm.dll`) regardless of import order, so heavy GPU stages must live in separate venvs (MinerU → .venv/torch, PaddleOCR → venv-ocr/paddle, TTS → venv-vieneu/torch); when an OCR script must call paddle from an env that lacks it, the assistant should relaunch the script under the correct venv via subprocess rather than installing conflicting frameworks together. Confidence: 0.6
- When asked to "nghiên cứu repo này" (research this repo) for an external GitHub project, expects the assistant to autonomously clone it, dig into the actual source structure (module-by-module, reading key files — not just the README), and deliver a structured Vietnamese report: overall architecture and module responsibilities/interconnections, core mechanisms, notable metrics (LOC, file/test counts, benchmarks), and an "interesting points compared to my project" section assessing relevance to the user's own work — the user asked for research on codebase-memory-mcp (a pure-C MCP server) and this was the delivered format; consistent with the user's recurring "nghiên cứu xem cần cải thiện gì" knowledge-gathering habit. Confidence: 0.55
- When considering whether to adopt an external tool, evaluates it on practical fit and token/cost savings for their OWN project, not just on its feature list — immediately after a features overview of codebase-memory-mcp, the user asked "dự án này có ổn với dự án tôi không có tiết kiệm token cho tôi không" (is this compatible with my project, does it save tokens?); expects the answer to measure the user's actual project scale empirically (~51 source files, ~15K LOC) and deliver a direct verdict — including an honest "KHÔNG phù hợp — bạn không cần nó" (not a fit, you don't need it) when the tool's benefits only kick in at far larger scale (e.g. the ~99% token savings benchmark applies to 28M-LOC codebases, not theirs) — rather than a feature-pushing sales pitch. Confidence: 0.55
- Is open to trialing an external tool hands-on even after the assistant's analysis concludes it is not a fit for the project — after the honest "KHÔNG phù hợp — bạn không cần nó" verdict on codebase-memory-mcp, the user replied "cài thì cũng ko sao đúng ko" (installing it wouldn't hurt, right?), treating a free, local, easily-uninstalled tool as zero-risk to try anyway; the assistant should support a low-cost trial install (e.g. `--skip-config` to avoid touching agent configs) rather than treating the negative verdict as the end of the discussion. Confidence: 0.6
- Before authorizing a new tool install, wants to understand its installation scope — project-local vs global — asking "cái này là cài trong dự án hay là tất cả dự án vậy" (is this installed in the project or across all projects?); expects the answer to clarify the footprint (binary + agent config scope vs per-project data such as indexes, which stay separate per repo) so the user can decide whether the tool affects only the current repo or everything. Confidence: 0.55
- During pre-install due diligence on an external tool, asks for BOTH its internal working mechanism (how it operates step-by-step, e.g. the 3 phases: index → query → maintain) AND a concrete itemized list of what the install actually delivers (binary location, the exact tool set grouped by purpose, UI extras, skills/agent profiles) before deciding — "cách thức hoạt động của dự án này ra sao và khi tôi cài tôi sẽ được cái gì" (how does this project work and what will I get when I install it); expects the explanation to make the mechanism tangible and the deliverables concrete rather than a generic feature list, as part of their multi-question evaluation (features → fit/token savings → risk → scope → mechanism/deliverables → agent compatibility). The agent-compatibility step is an explicit question — "dự án này có cài được với CommandCode không" (can this be installed with Command Code?) — expecting a documentation-backed answer (the assistant verified against Command Code's own MCP docs) with the exact registration path for their agent (`cmdc mcp add` or a project `.mcp.json`), including explicitly flagging when the tool's own installer covers other clients but NOT Command Code and manual registration is therefore needed. Confidence: 0.6
- Prefers to defer known, non-blocking limitations/issues to a later time rather than fixing them in the current session — when told that 3 books without EPUB files would error on the "Đọc thử" button, replied "ok cái này tôi lưu ý để sau hãng làm" (ok, I'll note this to do later), accepting the current behavior and expecting the assistant to acknowledge the issue, record it for future work, and move on without implementing a fix. Confidence: 0.55
- Communicates UI requests — both bug reports and feature/layout changes — as a single screenshot (often with an annotated red box) plus a brief Vietnamese description (e.g. bug: "phần chữ trong phần đọc thử sách epub ko nhìn thấy gì trằng tinh như hình"; feature: "chuyển tab realtime log sang bên phải xong có nút bấm bấm sang hoặc thu về giống nút ba gạch [Image #1] trong khung đỏ") — or sometimes text-only, e.g. "tôi chỉ chuột vào cái thanh process ... nó cứ bị giật giật kiểm tra lại cho tôi xem" — expecting the assistant to run the full reproduction/diagnosis/fix autonomously rather than asking clarifying questions. Also answers the assistant's diagnostic clarification questions by sending a screenshot of whatever is on screen plus a minimal caption ("đây bạn" — here you go), even when the screenshot does not actually contain the value being asked about (e.g. sent the summary screen, not the log line showing `cuda:0`), rather than describing what they see in words. Confidence: 0.9
- All Vietnamese text in UI and code strings must use proper diacritical marks (dấu) — explicitly complained "toàn tiếng việt không dấu" (all Vietnamese without diacritics) as unacceptable; expects "Đã tải", "Lỗi", "Chưa nhập" not "Da tai", "Loi", "Chua nhap". Confidence: 0.9
- Python scripts that print Vietnamese must reconfigure stdout to UTF-8 on Windows — the default console cp1252 encoding cannot output diacritical Vietnamese characters (slice_manifest.py output broke when printing Vietnamese slice names); the established project convention is `sys.stdout.reconfigure(encoding="utf-8")` at script start, and it is a known pattern already present in the project's other scripts. Confidence: 0.85
- Wants no PowerShell/console window to flash when running Python pipelines — explicitly asked "lúc chạy Python nó cứ hiện cái bảng PowerShell có thể tắt đi được không" (a PowerShell window keeps popping up when Python runs, can it be turned off?); expects every `subprocess.run`/`Popen` inside scripts (e.g. ffmpeg convert/merge in audiobook_long.py) to suppress the child console on Windows via a shared helper setting `subprocess.CREATE_NO_WINDOW`, in addition to the desktop app already using `CreateNoWindow = true` for the Python process itself — this is a permanent code convention, not a per-call workaround. Reconfirmed when the user again flagged an empty PowerShell window popping up during a background pipeline run (sent a screenshot: "có cái bảng này nó cứ hiện này") — the no-window requirement applies to every launch path, including assistant-started background jobs, not just subprocesses inside scripts. Confidence: 0.95
- Prefers thorough, evidence-backed code analysis over quick summaries — when reviewing code, expects actual source-file reading at relevant line ranges, verified file states (sizes, output), and prioritized breakdowns with severity levels rather than surface-level observations. The evidence standard includes empirically validating path/root resolution hypotheses with real file listings and small runnable reproduction programs (see the "Đọc thử" directory-trace episode) before drawing conclusions, so the report separates verified facts (which books have EPUBs and why) from hypotheses. Confidence: 0.9
- Prefers systematic progress tracking for multi-step tasks — accepts and expects todo lists or checklists to track work across many fixes/features. Confidence: 0.8
- Wants verification/testing after changes are made — explicitly asks to run tests or samples to confirm fixes work, doesn't just trust the code changes alone; also wants new features/workflows verified with real end-to-end runs (e.g. asked to "kiểm tra chức năng Dual-Agent" and "tôi sẽ thử và bạn chạy xem nhé" before relying on the feature; after the EPUB Preview compile fix, asked "donet build cho tôi" (build it for me) to have the assistant run the build and confirm the fix rather than trusting the code change alone). Reconfirmed with the terse "test thử hoạt động xem nhé" (test whether it actually works) right after a batch of changes was declared done — a functional end-to-end test pass of the changed flow (including a fake-book merge + state restore) was expected before considering the work finished). Confidence: 0.95
- Wants the actual runtime entry point of a UI feature traced and confirmed rather than assuming a fixed code path is the live one — asked "kiểm tra xem phần đọc thử đang trỏ đến đâu" (check where the preview button actually points) after a compile fix, expecting the assistant to trace UI wiring (XAML `Click=` handler vs `Command=` binding, code-behind vs ViewModel command) and report which entry point is really triggered, so fixes target the live path and unbound/dead commands are flagged, not silently treated as the feature. Reconfirmed with a follow-up diagnostic question "phần đọc thử Epub xem đang trỏ đến thư mục nào ấy" (check which directory the EPUB preview points to) — the expected answer traces the full chain (command → ViewModel → project-root resolution → per-book EPUB discovery) AND verifies it empirically against the real filesystem (listing actual `*.epub` files per book, running a scratch console test to confirm the root-path resolution behavior) rather than reasoning from code alone; the assistant's initial wrong hypothesis about the root directory was corrected by the empirical run before reporting. Confidence: 0.8
- Prefers MVVM command bindings over code-behind event handlers for WPF UI actions — when the assistant proposed converting the "Đọc thử" button from `Click="PreviewEpub_Click"` code-behind to a `Command="{Binding ...}"` binding (via RelativeSource ancestor to Window DataContext) and removing the now-unused handler as dead code, the user approved with "ok"; expects UI actions wired through ViewModel commands rather than scattered code-behind click handlers. Confidence: 0.65
- Wants proactive code quality suggestions even when code is working — asks "has this stabilized, what needs improvement?" and "cần cải thiện gì nữa không" (what else needs improvement?) rather than accepting functional code as-is; expects the assistant to analyze and surface edge cases, redundancies, and design issues unprompted; repeats this request after every completed batch of work, treating each finished milestone as a cue for the next gap review (fresh instances: "nghiên cứu tiếp xem cần cải thiện gì nữa không" right after a completed implementation batch, then "mình có cần nâng cấp gì nữa không" after the next completed batch, then "phần trộn nhạc nền theo bạn cần cải thiện gì không" asking for an improvement audit of the BGM-mixing demo before pipeline integration). Confidence: 0.99
- Strongly prefers generalizable, reusable fixes over one-off patches — explicitly instructs that changes should be made at the core processing layer so they apply to all inputs (e.g. all books), not just the current one being tested. Reconfirmed with an explicit thorough-fix order when reporting a TTS defect: "hãy khắc phục triệt để lỗi một câu nhưng nói tận 2 lần" (thoroughly fix the bug where a sentence is spoken twice) — the expected fix eliminates the entire bug class at the root layer (chunking), applies to all chapters, and is verified across every regenerated output (0 short chunks remaining across all 12 chapters, all chapters re-run and re-checked), not just the reported occurrence. Confidence: 0.9
- Prefers slow, warm, deep narration style for audiobook TTS — explicitly requested "chậm rãi và trầm ấm" (slow and warm/deep), chose temperature=0.3 and top_k=10 (the most conservative option) for a calm, even delivery; reinforced when the assistant kept these same parameters while re-running a chapter after a pronunciation fix ("vẫn giữ tham số như cũ: temp 0.3, top_k 10"). Confidence: 0.95
- TTS text chunking must never split mid-sentence — user complained about unnatural pauses ("hết hơi") when chunks break a sentence like "mê [pause] xám"; expects chunks to end at sentence-ending punctuation (.!?…;) or at least at clause boundaries (,;:) rather than mid-word/mid-phrase. Confidence: 0.9
- Delegates git operations to the assistant — expects autonomous handling of status checks, staging, and commit messages without step-by-step guidance; a terse request like "push lên github cho tôi nhé" (push to GitHub for me) means the assistant should run the whole status/stage/commit/push flow itself. Freshly reconfirmed with a near-identical terse order "push lên github cho mình" — the assistant immediately ran `git branch --show-current` / `git status --short` / `git diff --stat` and moved to proposing the commit organization, with zero clarifying questions. Confidence: 0.92
- Never auto-push to GitHub unless explicitly told — only commit locally; wait for the user to say "push" before pushing. Reconfirmed when the user authorized both at once with "commit và push gihub đi" — pushing is only ever done after an explicit "push"/"đi" instruction, never assumed from a commit request alone. Confidence: 0.97
- Pushes to GitHub with a single terse instruction that bundles commit + push ("commit và push gihub đi" — commit and push to github; "push github giúp tôi" — push to github for me; "push lên github cho mình" — push to github for me) without specifying any message or split, leaving message wording and commit organization entirely to the assistant; the assistant runs the whole status/stage/commit/push flow autonomously and only pauses to have the proposed scope-split commit messages approved first — freshly reconfirmed when the bare "push lên github cho mình" was answered with a 4-commit scope plan (code-TTS / code-translation-QA / config-commands / docs) presented as an approval table before any commit was made. Confidence: 0.9
- Taste files are kept out of git only as a default convention, not a hard rule — when the assistant listed the two `.commandcode/taste/*` files as the sole uncommitted items and asked "Bạn muốn không?" (do you want to push them?), the user explicitly replied "commit và push lên cho tôi nhé" (commit and push for me), authorizing the taste files into the repo (commit 63b7ab0) and expecting a clean git status afterward; the assistant should surface uncommitted taste files and let the user decide rather than assuming they must stay out of git. Confidence: 0.75
- The project's git repo follows a code-only commit policy — scripts, configs, and docs are committed, while product/generated data (e.g. glossary CSVs like `glossary/authors/van-tinh.csv`) is deliberately NOT committed; the assistant treated this as standing policy when logging the new author-glossary pool as "KHÔNG commit (sản phẩm, theo chính sách repo code-only)" while proposing the merge script + docs/config for commit. Confidence: 0.5
- Prefers to explicitly authorize local commits — after a task the agent drafts and proposes a commit message and waits for the user to say "commit" before actually committing locally; it does not auto-commit even locally (pushing still additionally requires a separate "push"). When the assistant reports being blocked by permission rules, the user grants blanket verbal authorization ("tôi cho phép bạn" — I allow you) expecting that to unblock execution of the pending git flow, rather than taking over the commands themselves. Confidence: 0.9
- Prefers cleanly separated, logically-grouped commits over a single monolithic commit — when the assistant proposed splitting the changes into two coherent commits (one for dual-agent config, one for desktop UI) and explicitly asked whether to merge into one, the user approved the split with "duyệt tách commit"; expects staged changes to be organized into per-scope commits (config vs feature/code) rather than one big commit. Confirmed again on a later push: after the user approved a two-commit split, the assistant autonomously organized a subsequent batch into a per-scope multi-commit sequence (desktop UI commit → audiobook script commit → docs/taste commit) and pushed all of them without re-asking — the user accepted the grouped commits without objection, treating terse "push lên github cho tôi" as authorization for the whole staged/split flow. Reconfirmed with a fresh, even larger split: when the assistant proposed a 5-commit scope plan (glossary master / commands / scripts / desktop fix / docs+.gitignore) with full per-commit messages and asked "Bạn duyệt tách theo 5 commit này không", the user approved the exact proposal verbatim with the terse "làm 5 conmmit như bạn nói" (make the 5 commits as you said) — and the assistant executed all five commits plus the push autonomously with zero further approval or message-editing from the user. Confidence: 0.85
- Prefers analysis and proposals before implementation for code changes, not just structural ones — explicitly says "phân tích đưa cho tôi đề xuất" (analyze and give me a proposal), "đọc dự án và đưa ra báo cáo đề xuất", and "đọc dự án đề xuất cho tôi tăng hiệu suất"; wants to review and approve the plan first, never just executes reorganization directly. This applies to the follow-up improvement research pass too: after finishing one batch of work, the next review ("nghiên cứu tiếp xem cần cải thiện gì nữa không") must present a prioritized proposal and wait for approval before editing any code — the assistant explicitly paused with "Tôi chưa sửa gì, để bạn duyệt trước" (I haven't changed anything, let you approve first), framing it as "theo đúng sở thích của bạn" (per your preference). Reconfirmed with a fresh instance: after the assistant's GitHub survey concluded no external tool was needed and offered a deeper analysis of self-implementable improvements, the user ordered "hãy phân tích đi xong có giải pháp đề xuất nâng cấp nào không" (go ahead and analyze, then are there any proposed upgrade solutions?) — the assistant read the actual project files (AGENTS.md, STATE.md, scripts, git state) for evidence and delivered a prioritized proposal table (Cao/Trung bình/Thấp with risks) ending with "Tôi chưa sửa gì — để bạn duyệt trước", and the upgrade suggestions were expected to be self-implementable no-new-tool items rather than adopting external repos. Freshly reconfirmed on the translation-quality request "làm để AI nó dịch mượt mà và văn chương lại láng như nhà văn": the assistant first ran a root-cause analysis (3 structural causes: chunk-level translation losing voice consistency, no quality reference samples, no revision pass), presented a prioritized 4-item proposal table (per-book literary profile / before-after examples / re-read-and-polish pass / doc update) with costs and risks and a recommended order, and closed with "Bạn duyệt làm nhóm nào?" — nothing was edited before approval, exactly per this preference. Confidence: 0.98
- Approves prioritized proposal items by NUMBER, non-contiguously, in terse shorthand — when the assistant proposed a 4-item plan for "dịch láng như nhà văn" (1. per-book literary profile, 2. before/after "cứng vs láng" examples, 3. re-read-and-polish pass, 4. doc update) with a recommendation to do the high-priority group first, the user replied "vậy làm 124 đi" (so do 1, 2, 4) — approving specific non-contiguous items by their numbers and silently deferring the medium-priority #3 (the slower revision pass) without comment; the assistant then implemented exactly those numbered items (create_book_profile.py + before/after examples in both prompt branches + step F2 in both dich.md registries, synced) with zero further questions. Confidence: 0.9
- After approving a proposal, delegates full end-to-end implementation to the assistant — after explicitly approving the plan with "ok triển khai theo plan này", expects the assistant to execute the approved scope completely (including setup and verification) rather than re-litigating it mid-way; also approves proposals with short confirmations like "thêm quy tắc đó cho tôi" (add that rule for me), "có nhé" (yes, sure), and "ok sửa giúp tôi nhé" (ok, fix it for me) and expects immediate implementation with no further questions. When offered a menu of improvement options (A/B/C), replies "làm khuyến nghị của bạn đi" (do your recommendations) or "ok đó bạn làm tất cả cho tôi xem nhé" (ok, do everything for me to see) — meaning implement ALL recommended items, not just a single selected one, and show the result. Approval can also be scoped by priority tier from a prioritized proposal table — "triển khai trước (nhóm ưu tiên cao + trung bình):" (implement the high + medium priority group first) means execute the entire chosen tier's full scope (all items in that tier) in one autonomous pass, deferring the lower tier without further questions. Within a tier the user may also curate by fix speed/safety: when the Medium tier held two quick items (search by display name, cancel button in overlay) plus one complex item (CSS background-image handling), the user approved "làm 2 mục Cao (fix mất log + Ctrl+F) + 2 mục TB (search theo tên + nút Hủy overlay) trước — đều nhanh" (do the 2 High + 2 Medium items first — they're all quick), explicitly deferring the complex same-tier item; expected the assistant to then implement exactly that curated scope autonomously (todo tracking → edit → kill running app → build → launch → verify) with no further questions. Confidence: 0.99
- When implementing structural changes, expects ALL related files updated simultaneously and project verified to still work — scripts, configs, .gitignore, docs must all be updated in one pass; explicitly says "nhớ cập nhập cho tôi tất cả các thứ liên quan để đảm bảo dự án tôi vẫn chạy mượt mà" (update everything related to ensure the project still runs smoothly). Reconfirmed when restructuring the output-directory layout: asked "kiểm tra đảm bảo khi cấu trúc lại thư mục như chúng ta đang làm các file liên quan phải được cập nhập nhé" (make sure all related files are updated when we restructure directories) — expects a consistency sweep across scripts, desktop code, and docs that greps for stale references to the removed structure/names (e.g. `trilingual.epub`, `final/vi.epub`, `final/tamngu.epub`), confirms nothing still depends on them, then syncs the rules into the pipeline docs (dich.md, session_log) and keeps the two command registries (.opencode + .commandcode) in sync. Confidence: 0.95
- Prefers project status/proposals presented as comparison tables — explicitly asked "đề xuất theo bảng để tôi xem" (propose in a table so I can see) and later "cho tôi một bảng đầy đủ" (give me a complete table); expects a tabular layout with columns like Hạng mục/Current state/Đề xuất/Ưu tiên/Rủi ro plus a concise recommended path of action, rather than prose-only explanations. Confidence: 0.85
- Expects project overviews/proposals to be complete across all pipeline areas — when reviewing a status table, flagged missing coverage ("còn thiếu phần audio nữa") and expects audiobook/TTS, publishing, desktop, and API sections included alongside translation/QA even when not explicitly mentioned; wants the full picture, not just the stage asked about. After implementation, a "tổng thể" (overall) review is expected to span desktop build, git state, and all real outputs, not just the changed scripts. Confidence: 0.85
- Reviews plans/proposals using structured tables with severity levels and a concrete proposed fix paired with each issue, rather than free-form critiques — e.g. columns like severity/issue/proposal; each problem identified comes with an actionable fix and a priority, so the review is immediately actable and ranked, not just a list of complaints. Confidence: 0.8
- Session/memory log files follow an append-only convention: new session entries are always added at the END of the file, preserving chronological history without reordering or prepending prior entries. Confidence: 0.7
- Wants a final pre-commit verification gate: when work is declared complete, the user will ask "kiểm tra một lần nữa xem đúng chưa" to trigger a consolidated re-check — cross-file consistency, binary file-identity/sync verification (e.g. fc /B), a live isolated E2E smoke test, and cleanup of stray test artifacts — before approving any commit. Confidence: 0.8
- When the user believes they manually changed the filesystem between sessions ("bạn kiểm tra lại cho tôi một lượt có một số file tôi xoá rồi hay sao ấy" — check again, I think I deleted some files), expects the assistant to re-verify the current state against the previously reported baseline — per-path existence checks, `git status`, recent commit log — and reconcile any discrepancy (e.g. explaining that files showing as modified disappeared from status because they were COMMITTED, not deleted) rather than just re-listing the current state; the re-check must produce a definitive, evidence-backed verdict on what is actually still there. Confidence: 0.55
- When reviewing a WPF view/window, expects BOTH the `.xaml` markup and the `.cs` code-behind to be examined together as one unit — after a review covered only `EpubPreviewWindow.xaml.cs`, the user reminded "à tôi quên mất ... xem cả 2 cái này" (oh I forgot, look at both); UI-side issues (e.g. `DefaultBackgroundColor`, styling, bindings, visibility logic) live in the XAML and must be included in the assessment alongside the code-behind logic. Confidence: 0.6
- Dislikes hard-coded file names/paths in code — flagged the "Đọc thử" preview pointing to a fixed `trilingual.epub` as "hơi cứng nhỉ" (a bit rigid) and incorrect for English-translated books (which produce `final/vi.epub`); expects file resolution to adapt to per-language output conventions via priority-based discovery (e.g. `trilingual.epub` for ZH → `final/vi.epub` for EN → any `.epub` fallback), applied consistently across all places that need the file, rather than assuming a single hard-coded filename. Confidence: 0.85
- Delegates documentation maintenance proactively — "xem readme cần cập nhập gì thì cập nhập cho tôi nhé" (check what the README needs updating and update it) means the assistant should autonomously audit the README against the actual project state — stale references to deleted files/components, missing new items (books/features), incorrect output-file conventions, missing new directories, outdated tool names — fix all discrepancies directly without a proposal step (docs updates are routine maintenance, unlike structural changes), then commit and push. Confidence: 0.8
- Quality-checks generated audiobook audio by listening and reports perceptual TTS defects — e.g. "một số câu bị đọc lại 2 lần nhưng thực tế trong file md chỉ có một câu thôi" (some sentences are read twice even though the source md has them only once); expects the audio to faithfully reproduce the source text, and expects the assistant to root-cause whether the defect comes from the source text/chunking (verify no duplicate lines/chunks, no chunk overlap) or from TTS model hallucination/repetition, then fix at the correct layer rather than assuming the audio matches the text. When reporting a defect, pinpoints the exact timestamp range and quotes the sentence verbatim (e.g. "ở chỗ 1 phút 37- 1 phút 39 giây 'Anh mà là người lớn'") — expecting the assistant to map that precise position to the exact chunk and root-cause from there (e.g. discovering the short dialogue-line chunk lacked context) rather than re-listening or guessing. Reconfirmed with a fresh defect report pinning an exact range ("ở 3 phút 45-3 phút 48 vẫn bị lặp 2 lần câu") plus a phonetic pronunciation example; a further report quoted the offending sentence verbatim with no timestamp ("vẫn bị lập chỗ câu 'Một số đứa ở lại lớp bảy'") and expected the assistant to map it to the exact chunk and root-cause from there. Confidence: 0.93
- Persists with a defect report until the root cause is actually fixed — when a first fix (embedding a font into the EPUB) failed to remove the `?` glyphs, the user reported the same symptom again ("vẫn bị lỗi ? ở chỗ tiếng việt nhé" — still broken at the Vietnamese part) plus a pointed tooling question ("bạn đang tạo epub bằng gì" — what are you using to create the EPUB), signaling the previous diagnosis was wrong and expecting a full re-investigation of the data path (source translation → merge → EPUB) rather than a repeated/re-tuned version of the same fix. Confidence: 0.6
- Treats unclear pronunciation of the initial consonant at the start of a new sentence/paragraph as a TTS defect on par with repetition — reported "Số" being heard as "Ố" (the leading consonant swallowed by the model when a chunk starts at a sentence/paragraph boundary); expects chunking/generation-layer fixes (e.g. merging chunks so new sentences don't begin at chunk boundaries, raising MAX_CHARS) so word-initial consonants are clearly articulated, not accepted as an inherent TTS limitation. Confidence: 0.8
- When verifying whether a TTS repetition/hallucination fix actually worked, wants a LONG continuous test audio generated from the full chapter text (entire chapter joined into one stream) rather than short snippets — explicitly asked "bạn tạo ví dụ chương 1 cho nó dài ra mới biết được so lặp hay không" (make a longer chapter-1 example so we can tell if it repeats); short ~60s clips were considered insufficient to judge repetition, so the assistant should build a dedicated long-form test slug/audio (plus a quick 2-min excerpt for convenience) for listening-based verification. The user's verdict on a fix comes from listening to the full regenerated chapter, not from metrics alone — a terse "chạy thử lại chương 1 tôi nghe thử" (run chapter 1 again, I'll listen) means regenerate the complete chapter audiobook with the current pipeline config and hand it over for a listening pass; the pattern recurred verbatim with "xong chưa cho tôi nghe thử chương 1" (done yet? let me hear chapter 1) right after a chunking-config change, confirming the user's verdict is a listening pass on the full regenerated chapter. Confidence: 0.85
- When a reported TTS defect (e.g. repeated sentences) survives parameter changes (MAX_CHARS 240→200, repetition_penalty 1.2→1.5), expects a scientific chunk-level root-cause pass instead of more blind tuning: keep the per-chunk WAV cache (added a `--keep-chunks` flag to the generator for this), measure each chunk's actual duration and duration-per-character ratio to detect doubled/abnormal chunks, verify the TTS engine's internal sub-chunking (`normalize_to_chunks_v3_with_gaps`) produces no boundary overlap, and only then conclude whether the defect is model-level short-sentence repetition vs a join/sub-chunk bug; confirms by listening to concatenated suspect-chunk samples rather than guessing. Confidence: 0.7
- "Background music" in this project means music mixed INTO the audiobook audio itself, as a "music bed" behind the narration voice — music plays under the TTS voice (quieter while the voice is speaking, swelling up in pauses, i.e. ducking by voice loudness) — NOT a standalone music player that plays alongside silent reading; the user explicitly corrected a standalone-player interpretation with "ý tôi là nhạc nên sau giọng của audio book cơ" (I meant the music should be behind the audiobook's voice). Confidence: 0.9
- When evaluating a new audiobook feature (e.g. background-music mixing) on a real book, prefers a trial of a few real chapters of a specific book rather than a synthetic demo — terse request "làm cho tôi thử 3 chương của một cuốn output\books\zuo-yi-ge-gang-gang-hao-de-nu-zi-wan-qing" (make me try 3 chapters of this book) means run the real pipeline (TTS + music + volume) on those chapters of that book's actual text so the result is directly listenable, before authorizing the full-book run. Reconfirmed with a fresh verbatim-consistent instance: "tạo thử cho tôi audio 3 chương output\books\ban-co-nam-cho-ngoi của sách này" (make a trial audio of 3 chapters of this book) — again a trial of a few real chapters of a specific named book. Confidence: 0.7
- Background music in audiobooks must stay clearly subordinate to the narration voice — a subtle bed at a low fraction of the music's native amplitude (volume ≈ 0.12 ≈ 5–12% after the amp fix; later tuned up to 0.18 — ~7% during speech / ~18% at pauses — when the user asked "cho tôi to hơn một tý" (make it a bit louder), so the level is tunable by ear while staying subordinate), so the result sounds like ONE mixed audiobook track rather than two parallel tracks; when the music was accidentally applied at 40–100% of native volume, the user immediately flagged it by ear ("nhạc nền to thế với cả là sao bảo trộn music không thấy trộn" — the background music is too loud, and why does it not even sound mixed?), expecting the bug to be fixed in the mixing code itself (volume must multiply the ducking ratio: `amp = volume * ratio`) and re-verified by measuring the actual audio, not just by re-tuning the volume parameter. The user continues to re-tune the level by ear and each settled value becomes the STANDING standard for all future books — the current standard (08-13): volume 0.15 (reduced from 0.20 because the user asked for music "nhỏ hơn một chút", a bit quieter), keeping ducking MIN_RATIO 0.50 and loudness normalize RMS 0.18; new-book runs should use these values without re-asking. Freshly reconfirmed when the user listened to the 0.15 re-run and approved by ear ("tôi thấy nhạc nền ok rồi đấy" — the background music is fine now), explicitly locking 0.15 in as the standing standard for future books. Confidence: 0.9
- Prefers per-book `metadata.json` files to be rich and self-describing rather than minimal — when asked what else the metadata should hold, approved expanding the schema beyond `slug/title/source_file` to include `author`, `language`, `genre`, `has_audio`, `has_epub`, `epub_file`, and `created`; fields that can be derived from the filesystem (`has_audio`, `has_epub`, `epub_file`) should be auto-detected from the folder at update time, never hand-maintained, and the full schema should be codified as a mandatory rule in the pipeline docs (dich.md) so future books generate it. Also, before mass-overwriting data files like metadata.json, expects a quick confirmation of the proposed field set ("Tôi xác nhận nhanh với bạn trước khi ghi đè") rather than silent edits. Confidence: 0.65
- Background music for audiobooks must be sourced ONLY from the project's `core/music` directory — the user modified the core themselves to enforce this ("trong core tôi đã thay đổi chỉ dùng các music có trong core ko được dùng cái khác" — in core I've changed it: only use the music present in core, don't use any others) and expects the pipeline to auto-discover and restrict itself to whatever music files exist there; when the user adds/removes/renames files in `core/music`, the pipeline must adapt to exactly that set and never pull tracks from elsewhere. Reconfirmed when, after finalizing the music-bed feature ("ngon rồi chốt cái này nhé"), the user announced "tôi sẽ đi kiếm thêm nhiều music nữa" (I'll go find more music) — expecting the pipeline to automatically pick up whatever tracks they drop into `core/music` (auto-discover, rotate per chapter, normalize loudness) with no code/config changes. Confidence: 0.85
- Holds the assistant accountable to previously agreed feature plans across sessions — when the delivered chapters all used the same background song despite the earlier agreement on per-chapter music, the user immediately flagged the discrepancy from memory ("hình như bạn bảo với tôi là mỗi chương 1 nhạc thì phải") rather than accepting the output; expects agreed feature decisions to actually materialize in the delivered output and to be corrected (with user confirmation for the re-run) when silently dropped. Confidence: 0.6
- Uses "kinh doanh/chất lượng thương mại" (commercial-grade quality) purely as a QUALITY BENCHMARK, not a real commercialization plan — the project is strictly personal-use: explicitly clarified "tôi thì không làm thương mại tôi chỉ làm cho cá nhân dùng thôi không kinh doanh gì cả, chỉ là so sánh bạn làm đã mượt như để kinh doanh chưa thôi" (I'm not doing commercial, I only make it for personal use, no business at all — I was just comparing whether what you've made is as smooth as if it were for business). So when the user asks "is this good enough to take into business?", answer the quality question directly with an honest benchmarked verdict (e.g. ~85-90% of commercial standard: audio/voice+BGM already commercial-grade, AI translation still needs a human editorial pass, pronunciation dictionary for proper nouns, 100% uniform quality, metadata/packaging polish) — and do NOT lead with copyright/legal-risk analysis or ranked commercial path options, since the user has no commercial intent; for personal use the output is declared more than good enough. Confidence: 0.6
- Wants only ONE EPUB per book — the trilingual root file named after the input source — with the tamngu (trilingual) and vi versions delivered as markdown only; explicitly stated "file tamngu và vi thì chỉ cần đến để md là được rồi không cần epub đâu" (tamngu and vi only need the md, no EPUB needed), so the pipeline must NOT generate `final/tamngu.epub` or `final/vi.epub` (those variants were deleted for all existing books and the rule codified in dich.md so future books keep just the single root EPUB + `final/*.md`). Confidence: 0.9
- Seeks a simple yes/no confirmation of a report's bottom line before acting on it — after the GitHub repo survey concluded no external tool is needed at the project's current scale, asked "như bạn báo cáo là tôi không cần nâng cấp thêm bất kì gì nữa đúng khônh" (so per your report I don't need to upgrade anything more, right?); expects the assistant to correct an over-simplified takeaway precisely rather than just agreeing — distinguishing "no new external repo/tool needed" from "nothing left to improve" — and to surface concrete self-implementable, no-new-tool improvements (mandatory memory-write checklist/verification in AGENTS.md, pre-commit guardrails against dangerous git patterns, shared glossary reuse across same-topic books) for the user to consider, consistent with their inclination to stop adding tooling once the current setup is deemed sufficient. Confidence: 0.5
- Wants long-running CLI generation jobs (e.g. audiobook TTS batch) to print progress INCREMENTALLY in real time — each batch line appearing as its group finishes, so the log steps forward line by line ("chạy từng dòng nhảy nhảy", described as "nhìn cho nguy hiểm" — looking cool), rather than buffering all lines until the whole chapter finishes and dumping them at once, and rather than a single-line `\r`-overwrite progress bar; the pattern "Generating..." followed by a super-fast scroll ("chạy vèo vèo") is explicitly unsatisfying ("nhìn nó ko đã"). The fix pattern: split the whole-chapter `infer_batch` into `batch_size` groups and print each group as it completes. Confidence: 0.85
- Asks for whole-project book-completion status reports ("lập cho tôi báo cáo xem cuốn sách nào làm rồi hay chưa làm giúp tôi" — make me a report of which books are done and which aren't) and expects the report to be compiled from an ACTUAL filesystem survey (per-book output dirs: final files, EPUBs, MP3 counts; progress JSONs; input dir), cross-referenced against the project docs rather than trusting STATE.md/session_log alone — explicitly flagging discrepancies between docs and disk (books missing from STATE.md, audiobooks present without extracted/chunks records, input files with no output folder) — and to categorize every book by completion state (fully done / translated-no-audiobook / in-progress / audio-without-translation-records / unprocessed input) with a summary and offered next-step options. Confidence: 0.55
- Prefers the smarter AI-based implementation over a free heuristic alternative once the cost is confirmed negligible — after the cost analysis, approved Level B (AI-chosen per-chapter music) over the zero-cost Level A heuristic with "ok thế làm mức B cho tôi cho nó thông minh" (ok, do Level B for me, make it smart), explicitly prioritizing intelligence over saving pennies; expects the assistant to proceed with the approved AI approach immediately rather than re-offering the cheap option. Confidence: 0.85
- Uses Deepseek as their AI provider of choice for AI-powered features — noted "dù sao tôi cũng đang dùng Deepseek" (I'm already using Deepseek anyway) when approving Level B, so AI integrations should reuse the project's existing Deepseek config (API key from `~/.translate_book/config.json`, model `deepseek-chat`, baseUrl `https://api.deepseek.com/v1`) with `DEEPSEEK_API_KEY` env-var fallback, rather than requiring the user to set up a new provider. Confidence: 0.8
- Wants audiobook background-music selection to be content-aware (match each chapter's mood/pacing to a fitting track) rather than just rotating — asked "ở phần audio bạn có thể phân tích được nhạc nền nào phù hợp với chương nào dựa vào nội dung chương đó không" (can you analyze which background music fits which chapter based on that chapter's content?), i.e. map chapter text features (emotion via Vietnamese sentiment keywords, dialogue/action density) to each track's audio features (tempo, dynamics, spectral brightness) so every chapter gets the most fitting music; expressed this as an open feasibility question and left the implementation approach (heuristic vs AI agent) open for the assistant to propose. Confidence: 0.6
- Wants full-project cleanup audits delivered as a categorized report BEFORE anything is deleted — asked "kiểm tra toàn bộ dự án hãy đưa ra cho tôi báo cáo dọn dẹp những file thừa file rác của dự án" (check the whole project and give me a cleanup report of redundant/junk files); expects the survey to cover every directory and evidence source (git status/untracked, .gitignore coverage, temp/backup file patterns, __pycache__, build artifacts, output test dirs, venv/tool dirs) and the report to separate three groups — (1) true junk that can be deleted right away (old test samples/artifacts, error logs, temp glossary prompts, crash logs), (2) .gitignore gaps that make junk show up as untracked in git (e.g. `*.log.err` missing while `*.log` is ignored, un-ignored working dirs), and (3) legitimate environment/product directories that must NOT be touched (venvs, node_modules, ffmpeg, build outputs, real book outputs) — with measured sizes per group and a recommended action plan; deletion and .gitignore fixes are only PROPOSED, executed only after the user approves, and the assistant explicitly does not commit until told. The same audit-then-approve pattern applies to targeted directory-cleanup questions — a terse "output\_archive cần dọn dẹp không" (does output\_archive need cleaning?) expects the assistant to survey that directory, categorize its contents with measured sizes, and present a confirmable plan (menu options with a recommended choice) before deleting anything; after the approved deletion, stale references to the removed directory in docs (AGENTS.md) must be purged so nothing points at non-existent paths. Confidence: 0.85
- Legacy/archive directories in the project get deleted outright, not retained — when asked whether `output/_archive/` (legacy backups of removed books, pre-rename copies, and a test-audio dir, ~9.5MB) needed cleaning, approved "Xoá toàn bộ" (delete everything) rather than keeping any archive copy; the project should not accumulate backup copies of deleted books or test artifacts, even for reference. Confidence: 0.7
- Background music tracks for audiobooks must not be mixed at one single fixed volume, because tracks are mastered at different loudness levels — the user flagged that a fixed volume is unreasonable ("sẽ có những bài music nhạc to có bài music nhạc nhỏ nếu fix volume như vậy không hợp lý lắm nhỉ" — some tracks are loud, some quiet, so fixing the volume isn't sensible) and expects per-track loudness normalization (measure each track's actual loudness/RMS → scale it to a common standard level → then apply the ducking ratios) so all chapters' music beds sound equally loud regardless of each file's native master, without the user having to edit the source music files. When the assistant proposed adding this RMS-normalization step to the mixing pipeline (measure whole-track RMS → scale to a common target → apply ducking), the user approved with the terse "có nhé" (yes, sure) and accepted the re-run of the chapters to verify uniformity. Confidence: 0.9
- Finalizes a completed feature with a terse sign-off phrase like "ngon rồi chốt cái này nhé" (good, let's lock this in) — a terminal approval that closes the feature: the delivered parameters/behavior become the standing standard, no further iteration is requested, and the assistant is expected to record the decision in project memory (session_log + STATE) and leave the commits pending the user's explicit authorization; the user then moves on to extending assets/content themselves rather than asking for more changes. Confidence: 0.5
- Plans to keep growing the audiobook background-music library by dropping new tracks into `core/music` over time — after locking in the music-bed feature, announced "tôi sẽ đi kiếm thêm nhiều music nữa" (I'll go find more music) and expects the pipeline to automatically pick up whatever new files land there (auto-discover the set, rotate per chapter, loudness-normalize) with no code or config changes, and the assistant to confirm that expectation rather than re-verifying each addition. Confidence: 0.5
- Prefers each audiobook chapter to carry a DIFFERENT background-music track, rotating through the available tracks in `core/music` (chapter N → track `(N-1) % number_of_tracks`) rather than reusing the same song for every chapter — reminded the assistant of this agreed feature when all 3 chapters came back with the same single song ("hình như bạn bảo với tôi là mỗi chương 1 nhạc thì phải" — didn't you say each chapter gets its own music?), then approved re-running the chapters with `--music` taking multiple comma-separated file names so each chapter mixes a distinct track, and confirmed the final progress metadata lists all rotated `music_files`. Confidence: 0.85
- Holds the assistant accountable to previously agreed feature plans across sessions — when the delivered chapters all used the same background song despite the earlier agreement on per-chapter music, the user immediately flagged the discrepancy from memory ("hình như bạn bảo với tôi là mỗi chương 1 nhạc thì phải") rather than accepting the output; expects agreed feature decisions to actually materialize in the delivered output and to be corrected (with user confirmation for the re-run) when silently dropped. Confidence: 0.6
memory ("hình như bạn bảo với tôi là mỗi chương 1 nhạc thì phải") rather than accepting the output; expects agreed feature decisions to actually materialize in the delivered output and to be corrected (with user confirmation for the re-run) when silently dropped. Confidence: 0.6
ilently dropped. Confidence: 0.6
- Prefers to keep the proven, stable TTS engine over switching to newer alternatives — after researching Gwen-TTS and VietVoice-TTS for better reading/audio quality, the user accepted the "stay with VieNeu" verdict immediately with "VieNeu là ngon lắm rồi" (VieNeu is already very good) and pivoted improvement effort to translation quality instead; expects honest "not worth switching" assessments that weigh maturity/risk (new repos with few commits, VRAM/driver requirements, fit with the established pipeline) rather than being pushed toward engine upgrades for marginal gains. Confidence: 0.55
- Expects new QA/detection features to be validated against a REAL completed book's output to prove their worth — after adding the free literary-quality checks (word repetition, machine-translation clichés, over-long sentences, Han-Viet ratio), the assistant tested them on an existing finished translation's `vi.md` and reported concrete findings (31× "một cách", 15× "đối với", 82 repeated-word spots) as evidence the checks catch real defects and future books will be flagged automatically; the user accepted the feature on this evidence, with the paid AI-fix level left as a later option pending their decision. Confidence: 0.7
